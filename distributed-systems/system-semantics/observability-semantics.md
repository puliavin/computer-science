# Observability Semantics (Семантика наблюдаемости)

## Оглавление

1. [Введение](#введение)
2. [Logging Semantics (Семантика логирования)](#logging-semantics-семантика-логирования)
   - [Что такое Logging Semantics](#что-такое-logging-semantics)
   - [Гарантии и ограничения](#гарантии-и-ограничения-логирования)
   - [Типы семантик доставки логов](#типы-семантик-доставки-логов)
   - [Проблемы и инциденты](#проблемы-и-инциденты-логирования)
   - [Практики проектирования](#практики-проектирования-логирования)
3. [Metric Visibility Semantics (Семантика видимости метрик)](#metric-visibility-semantics-семантика-видимости-метрик)
   - [Что такое Metric Visibility Semantics](#что-такое-metric-visibility-semantics)
   - [Гарантии и ограничения](#гарантии-и-ограничения-метрик)
   - [Типы метрик и их семантика](#типы-метрик-и-их-семантика)
   - [Проблемы и инциденты](#проблемы-и-инциденты-метрик)
   - [Практики проектирования](#практики-проектирования-метрик)
4. [Tracing Semantics (Семантика трассировки)](#tracing-semantics-семантика-трассировки)
   - [Что такое Tracing Semantics](#что-такое-tracing-semantics)
   - [Гарантии и ограничения](#гарантии-и-ограничения-трассировки)
   - [Контекст и причинность](#контекст-и-причинность)
   - [Проблемы и инциденты](#проблемы-и-инциденты-трассировки)
   - [Практики проектирования](#практики-проектирования-трассировки)
5. [Event Ordering Semantics (Семантика упорядочивания событий)](#event-ordering-semantics-семантика-упорядочивания-событий)
   - [Что такое Event Ordering Semantics](#что-такое-event-ordering-semantics)
   - [Гарантии и ограничения](#гарантии-и-ограничения-упорядочивания)
   - [Типы упорядочивания](#типы-упорядочивания)
   - [Проблемы и инциденты](#проблемы-и-инциденты-упорядочивания)
   - [Практики проектирования](#практики-проектирования-упорядочивания)
6. [Sampling Semantics (Семантика сэмплирования)](#sampling-semantics-семантика-сэмплирования)
   - [Что такое Sampling Semantics](#что-такое-sampling-semantics)
   - [Гарантии и ограничения](#гарантии-и-ограничения-сэмплирования)
   - [Типы и стратегии сэмплирования](#типы-и-стратегии-сэмплирования)
   - [Проблемы и инциденты](#проблемы-и-инциденты-сэмплирования)
   - [Практики проектирования](#практики-проектирования-сэмплирования)
7. [Взаимодействие семантик](#взаимодействие-семантик)
8. [Заключение](#заключение)

---

## Введение

**Observability Semantics** (семантика наблюдаемости) — это набор правил и гарантий о том, как системы собирают, передают, хранят и представляют данные о своей работе. Если представить распределенную систему как огромный город, то observability — это система камер, датчиков, репортеров и летописцев, которые фиксируют, что происходит в этом городе.

Но вот вопрос: можешь ли ты доверять этим данным? Всегда ли камера работает? Не потерялись ли какие-то записи? Показывают ли они события в правильном порядке? Это и есть вопросы семантики наблюдаемости.

⚠️ **Важно понимать**: observability — это не просто "мониторинг", это способность понять внутреннее состояние системы по внешним сигналам. А семантика определяет, насколько точно и полно ты можешь это сделать.

В реальном мире системы наблюдаемости не идеальны. Логи могут теряться, метрики могут быть неточными, трейсы могут быть неполными. Понимание этих ограничений критически важно для правильной интерпретации данных и расследования инцидентов.

---

## Logging Semantics (Семантика логирования)

### Что такое Logging Semantics

**Logging Semantics** (семантика логирования) определяет правила и гарантии о том, как логи генерируются, передаются и сохраняются. Это ответы на вопросы:
- Гарантированно ли каждое событие будет залогировано?
- Могут ли логи дублироваться?
- Могут ли логи теряться?
- В каком порядке логи будут записаны?

Представь, что ты владелец магазина, и у тебя есть журнал, куда кассиры записывают каждую продажу. Семантика логирования — это правила о том, как ведется этот журнал: гарантированно ли каждая продажа попадет в журнал? Могут ли быть дубликаты? Что делать, если кассир забыл записать или журнал потерялся?

### Гарантии и ограничения логирования

#### 🎯 Что гарантирует логирование

**Best-effort delivery** (доставка "по возможности"):
- Система попытается залогировать событие
- Нет гарантии доставки
- Самая распространенная семантика в production-системах

**Пример**: Nginx пишет access logs на локальный диск. Если диск переполнен или недоступен, логи просто теряются. Nginx не будет блокировать обработку запросов ради логирования.

#### ❌ Что НЕ гарантирует логирование

**Полнота данных**:
- Логи могут теряться из-за проблем с сетью
- Логи могут теряться из-за переполнения буферов
- Логи могут не записаться при внезапном падении приложения

**Отсутствие дубликатов**:
- При ретраях логи могут дублироваться
- При репликации логов могут появиться дубли

**Порядок событий**:
- Логи из разных источников могут приходить не в том порядке, в котором события произошли
- Даже логи с одного сервера могут перемешаться при параллельной записи

### Типы семантик доставки логов

#### 1. Fire-and-forget (Отправил и забыл)

Самая простая и быстрая семантика. Приложение отправляет лог и сразу продолжает работу, не ожидая подтверждения.

```
Application → [log message] → (network) → Logger Service
              ↓
         (continues)
```

**Реальный пример**: Приложение отправляет логи через UDP в syslog. UDP не гарантирует доставку — если пакет потерялся в сети, никто не узнает.

**Плюсы**:
- Минимальная задержка
- Не влияет на производительность приложения
- Простота реализации

**Минусы**:
- Логи могут теряться
- Невозможно узнать, дошел ли лог
- Нет гарантии порядка

#### 2. At-most-once delivery (Не более одного раза)

Система гарантирует, что лог будет отправлен максимум один раз. Если произошла ошибка, лог теряется, но дубликатов не будет.

**Реальный пример**: Приложение пишет логи в локальный файл. При ротации файла (переполнении) старые логи удаляются. Каждая строка записывается ровно один раз, но если процесс упал до записи на диск, логи теряются.

**Когда применяется**:
- Когда дубликаты хуже, чем потери
- Когда объем данных критичен
- Для отладочных логов, которые не критичны

#### 3. At-least-once delivery (Как минимум один раз)

Система гарантирует, что лог будет доставлен хотя бы один раз, но возможны дубликаты.

```
Application → [log] → Network → Logger Service
     ↓ timeout          ↓           ↓
   [log] ────────────> Retry → Disk (2 copies!)
```

**Реальный пример**: Приложение отправляет логи в Kafka. Если producer не получил acknowledgment от broker (возможно, из-за временной сетевой проблемы), он отправит сообщение снова. В итоге в Kafka может оказаться два одинаковых лога.

**Плюсы**:
- Не теряются критичные события
- Можно дедуплицировать на стороне получателя

**Минусы**:
- Нужна логика дедупликации
- Увеличенный объем данных
- Сложнее анализировать метрики на основе логов

#### 4. Exactly-once delivery (Ровно один раз)

Система гарантирует, что каждый лог будет доставлен ровно один раз — ни дубликатов, ни потерь.

**Важно**: Истинный exactly-once в распределенных системах достижим только при определенных условиях и с существенными компромиссами по производительности.

**Реальный пример**: Kafka с транзакциями и идемпотентным producer может обеспечить exactly-once семантику, но это работает только в пределах одного топика и требует координации между producer и broker.

**Проблема**: Это очень дорого по производительности и почти никогда не применяется для обычного логирования.

### Проблемы и инциденты логирования

#### Инцидент 1: Потерянные логи из-за переполнения буфера

**Ситуация**: Высоконагруженный API начал генерировать очень много логов (например, из-за включенного debug-режима). Логи отправлялись в in-memory buffer, который быстро переполнился.

**Что произошло**:
- Приложение начало отбрасывать новые логи
- В момент критической ошибки логи не записались
- При расследовании инцидента невозможно было понять, что пошло не так

**Почему это случилось**:
- Не учли объем данных при debug-режиме
- Buffer был слишком мал
- Не было backpressure mechanism (механизма обратного давления)

**Как избежать**:
- Использовать асинхронное логирование с достаточно большим buffer
- Мониторить dropped logs (отброшенные логи)
- Применять rate limiting для логов
- Критичные логи писать синхронно или с гарантией доставки

#### Инцидент 2: Дубликаты логов в metrics pipeline

**Ситуация**: Компания считала количество ошибок API, парся логи в ClickHouse. После миграции на Kafka с at-least-once семантикой метрики показали удвоенное количество ошибок.

**Что произошло**:
- Логи дублировались из-за ретраев в Kafka
- Система считала каждый дубликат как отдельную ошибку
- Дашборды показывали ложные алерты

**Как избежать**:
- Добавлять уникальный ID к каждому логу
- Дедуплицировать на стороне агрегатора
- Использовать идемпотентные операции при подсчете метрик
- Понимать семантику доставки всей цепочки

#### Инцидент 3: Out-of-order логи в распределенной системе

**Ситуация**: Микросервисная архитектура с 20+ сервисами. При расследовании инцидента логи показывали, что "ответ был отправлен раньше, чем пришел запрос".

**Что произошло**:
- Логи собирались с разных серверов
- Часы на серверах были не синхронизированы (drift до 5 секунд)
- Логи из разных сервисов приходили в log aggregator в разное время
- Timestamp в логах не соответствовал реальному порядку событий

**Как избежать**:
- Использовать NTP для синхронизации времени
- Добавлять logical clocks (логические часы) или vector clocks
- Использовать distributed tracing для связи событий
- Не полагаться на timestamp как на единственный индикатор порядка

### Практики проектирования логирования

#### 1. Структурированное логирование

Вместо текстовых логов используй структурированный формат (JSON, Protocol Buffers):

**Преимущества**:
- Проще парсить и индексировать
- Легче дедуплицировать
- Можно добавлять контекст (request_id, trace_id)

**Пример структуры**:
```
{
  "timestamp": "2024-01-15T10:30:00.123Z",
  "level": "ERROR",
  "service": "payment-service",
  "request_id": "req-12345",
  "trace_id": "trace-67890",
  "message": "Payment processing failed",
  "error_code": "INSUFFICIENT_FUNDS",
  "user_id": "user-42"
}
```

#### 2. Контекст и корреляция

Добавляй контекстную информацию к каждому логу:
- **Request ID**: уникальный идентификатор запроса
- **Trace ID**: идентификатор distributed trace
- **User ID**: идентификатор пользователя
- **Session ID**: идентификатор сессии

Это позволяет связывать логи из разных сервисов в единую историю.

#### 3. Log levels (Уровни логирования)

Правильно используй уровни логирования:

| Level | Назначение | Volume | Примеры |
|-------|-----------|---------|---------|
| DEBUG | Детальная отладочная информация | Очень высокий | Значения переменных, промежуточные состояния |
| INFO | Информация о нормальной работе | Высокий | Старт/стоп сервиса, обработка запросов |
| WARN | Потенциальные проблемы | Средний | Deprecated API, медленные запросы, retry |
| ERROR | Ошибки, требующие внимания | Низкий | Исключения, failed requests |
| FATAL | Критические ошибки, приводящие к остановке | Очень низкий | Невозможно подключиться к БД, out of memory |

#### 4. Buffering и batching

**Buffering**: Накапливай логи в памяти перед отправкой:
- Снижает количество network calls
- Улучшает производительность
- Но увеличивает риск потери при сбое

**Batching**: Отправляй логи пачками:
- Эффективнее использование сети
- Лучше для log aggregators
- Увеличивает задержку доставки

**Рекомендация**: Комбинируй buffering с flushing:
- Flush при заполнении buffer (размер)
- Flush по таймеру (время)
- Flush при критичных событиях (ERROR, FATAL)

#### 5. Fallback strategies (Стратегии отказоустойчивости)

Что делать, когда основной log destination недоступен?

**Local disk fallback**:
- Пиши логи на локальный диск
- Отправь позже, когда сервис восстановится
- Но следи за местом на диске

**Secondary destination**:
- Отправляй логи в альтернативный aggregator
- Например, если Kafka недоступен, пиши в local syslog

**Circuit breaker**:
- Если log service недоступен долго, переставай пытаться
- Избегай накопления retry queue
- Периодически проверяй доступность

#### 6. Log sampling для высоконагруженных систем

Если генерируется слишком много логов:

**Rate limiting**:
- Ограничь количество логов в секунду
- Например, логируй только каждый 100-й успешный запрос

**Dynamic sampling**:
- В нормальном режиме логируй 1% запросов
- При ошибках увеличь до 100%

**Пример**: Nginx обрабатывает 100,000 RPS. Логировать каждый запрос = огромный объем данных. Решение: логировать только ошибки + 1% успешных запросов для статистики.

---

## Metric Visibility Semantics (Семантика видимости метрик)

### Что такое Metric Visibility Semantics

**Metric Visibility Semantics** (семантика видимости метрик) определяет правила о том, когда, как и с какой точностью метрики становятся доступны для наблюдения. Это ответы на вопросы:
- Насколько актуальны метрики, которые ты видишь?
- Насколько точны агрегированные значения?
- Что происходит с метриками при сбоях?
- Можно ли доверять трендам и аномалиям на графиках?

Представь градусник за окном. Он показывает температуру, но:
- Обновляется раз в минуту (не в реальном времени)
- Округляет до целых градусов (не абсолютно точен)
- Может зависнуть и показывать старое значение
- Если отключить питание, история пропадет

Метрики в системах работают аналогично.

### Гарантии и ограничения метрик

#### 🎯 Что гарантируют метрики

**Eventual visibility** (Конечная видимость):
- Метрика рано или поздно станет видна
- Задержка (latency) может варьироваться
- Типичная задержка: секунды или минуты

**Approximate accuracy** (Приблизительная точность):
- Метрики часто используют приблизительные алгоритмы
- Для агрегации больших объемов данных
- Погрешность обычно приемлема

**Пример**: Prometheus scrape метрики каждые 15 секунд. Если событие произошло между scrape intervals, оно будет видно только на следующем scrape. Плюс есть задержка на обработку и отображение в Grafana.

#### ❌ Что НЕ гарантируют метрики

**Real-time visibility** (Видимость в реальном времени):
- Всегда есть задержка между событием и его видимостью в метрике
- Задержка может увеличиваться при высокой нагрузке

**Абсолютная точность**:
- Счетчики могут сбрасываться при перезапуске
- Агрегация может быть приблизительной (например, HyperLogLog для уникальных значений)
- Sampling уменьшает точность

**Полнота данных**:
- Метрики с failed hosts могут теряться
- При сетевых проблемах данные могут не доходить
- История метрик может быть ограничена (retention policy)

### Типы метрик и их семантика

#### 1. Counter (Счетчик)

Монотонно возрастающее значение. Используется для подсчета событий.

**Семантика**:
- Значение только растет (или сбрасывается в 0)
- При перезапуске обычно сбрасывается
- Анализируется через rate (скорость изменения)

**Реальный пример**: Количество обработанных HTTP-запросов в Nginx:
```
nginx_http_requests_total{status="200"} = 1,532,891
nginx_http_requests_total{status="500"} = 42
```

**Проблемы**:
- Counter reset: При перезапуске сервиса счетчик сбрасывается. Система мониторинга должна это детектировать и не показывать отрицательный rate.
- Overflow: Если counter достиг максимального значения (например, 2^64), он обнуляется.

**Что гарантирует**: Значение никогда не уменьшается (кроме reset).
**Что НЕ гарантирует**: Что каждое событие будет учтено (могут быть пропуски при сбоях).

#### 2. Gauge (Датчик)

Значение, которое может увеличиваться и уменьшаться. Используется для измерения текущего состояния.

**Семантика**:
- Отражает текущее значение на момент измерения
- Может быть любым числом
- Snapshot текущего состояния

**Реальный пример**: Количество активных соединений в PostgreSQL:
```
pg_active_connections = 47
```

Или использованная память в Redis:
```
redis_memory_used_bytes = 2,147,483,648
```

**Проблемы**:
- Если scrape пропущен, ты теряешь информацию о том, что было в этот момент
- Peak values (пиковые значения) между scrapes могут быть не видны

**Пример инцидента**: Memory leak в приложении. Память растет плавно, но scrape interval = 60 секунд. За это время memory достигла лимита и процесс был убит OOM killer. На графике ты видишь резкое падение, но не видишь, что память достигла лимита.

**Что гарантирует**: Показывает реальное значение на момент измерения.
**Что НЕ гарантирует**: Что ты увидишь все изменения между измерениями.

#### 3. Histogram (Гистограмма)

Распределение значений по buckets (корзинам). Используется для анализа latency, размеров запросов и т.д.

**Семантика**:
- Разбивает значения на предопределенные интервалы (buckets)
- Считает количество значений в каждом bucket
- Позволяет вычислять percentiles

**Реальный пример**: HTTP request duration в миллисекундах:
```
http_request_duration_ms_bucket{le="10"} = 5230      // <= 10ms
http_request_duration_ms_bucket{le="50"} = 8751      // <= 50ms
http_request_duration_ms_bucket{le="100"} = 9823     // <= 100ms
http_request_duration_ms_bucket{le="500"} = 10210    // <= 500ms
http_request_duration_ms_bucket{le="+Inf"} = 10342   // все значения
http_request_duration_ms_sum = 423891                // сумма всех значений
http_request_duration_ms_count = 10342               // количество измерений
```

**Проблемы**:
- Точность зависит от выбора buckets
- Если buckets выбраны неправильно, можешь потерять важную информацию

**Пример**: Твои buckets: [10, 50, 100, 500]. Если у тебя latency обычно 80-120ms, большинство значений попадет в bucket "100". Ты не увидишь детали распределения в этом диапазоне.

**Что гарантирует**: Приблизительное распределение значений.
**Что НЕ гарантирует**: Точные percentile значения (они вычисляются приблизительно на основе buckets).

#### 4. Summary (Сводка)

Альтернатива histogram. Вычисляет quantiles (квантили) на стороне приложения.

**Семантика**:
- Приложение само вычисляет percentiles (например, p50, p95, p99)
- Отправляет уже готовые значения

**Реальный пример**:
```
http_request_duration_seconds{quantile="0.5"} = 0.052   // медиана (p50)
http_request_duration_seconds{quantile="0.9"} = 0.158   // p90
http_request_duration_seconds{quantile="0.99"} = 0.389  // p99
http_request_duration_seconds_sum = 1823.4
http_request_duration_seconds_count = 42341
```

**Преимущества**:
- Более точные percentiles
- Меньше данных отправляется

**Недостатки**:
- Нельзя агрегировать quantiles из разных instances
- Больше вычислений на стороне приложения

**Проблема**: У тебя 10 instances приложения. Каждый считает свой p99. Ты не можешь просто усреднить эти значения, чтобы получить общий p99. Нужны raw данные или histogram.

### Проблемы и инциденты метрик

#### Инцидент 1: Missing metrics из-за service discovery lag

**Ситуация**: Микросервис автоскейлится в Kubernetes. Prometheus использует Kubernetes service discovery для поиска targets.

**Что произошло**:
- Запустился новый pod
- Service discovery обновляется каждые 30 секунд
- Pod начал обрабатывать traffic сразу
- Prometheus "узнал" о pod через 30 секунд
- Метрики из первых 30 секунд жизни pod потеряны

**Последствие**: При анализе метрик ты не видишь startup spike (пик нагрузки при старте), который на самом деле был причиной проблем.

**Как избежать**:
- Использовать push-based метрики для критичных событий
- Уменьшить service discovery interval
- Использовать warm-up period перед тем, как pod начнет получать traffic

#### Инцидент 2: Misleading aggregation (Вводящая в заблуждение агрегация)

**Ситуация**: Monitoring dashboard показывает "Average response time" всего API.

**Что произошло**:
- Один endpoint (/health-check) вызывается 1000 раз в секунду с latency 1ms
- Другой endpoint (/heavy-report) вызывается 1 раз в минуту с latency 30 секунд
- Average показывает ~2ms, все выглядит хорошо
- Пользователи жалуются на медленный /heavy-report

**Проблема**: Среднее значение не показывает реальной картины, когда есть высокочастотные быстрые операции и редкие медленные.

**Как избежать**:
- Использовать weighted average (взвешенное среднее)
- Смотреть на percentiles, а не средние
- Группировать метрики по endpoint
- Использовать histogram вместо простого average

#### Инцидент 3: Cardinality explosion

**Ситуация**: Разработчик добавил в метрику label с user_id для отладки.

**Что произошло**:
- У сервиса 10 миллионов пользователей
- Метрика стала создавать 10 миллионов time series (по одной на каждого пользователя)
- Prometheus начал падать из-за нехватки памяти
- Query стали медленными или таймаутили
- Storage быстро переполнился

**Проблема**: High cardinality (высокая кардинальность) labels разрушают performance monitoring системы.

**Как избежать**:
- Не использовать unbounded labels (user_id, request_id, email и т.д.)
- Использовать bounded labels (status_code, http_method, endpoint)
- Для детального анализа использовать logs или traces, а не metrics

#### Инцидент 4: Stale metrics после deployment

**Ситуация**: После deployment в метриках остались значения со старых instances.

**Что произошло**:
- Rolling deployment: старые pods постепенно заменяются новыми
- Старый pod перестал обрабатывать traffic, но еще жив
- Prometheus продолжает scrape метрики со старого pod
- На dashboard видны метрики из двух версий приложения одновременно
- Суммарные значения становятся некорректными (двойной счет)

**Как избежать**:
- Правильно настраивать lifecycle hooks в Kubernetes
- Использовать label для версии приложения
- Фильтровать метрики по активным instances
- Не держать terminated pods слишком долго

### Практики проектирования метрик

#### 1. RED Method (для сервисов)

**RED** = Rate, Errors, Duration

- **Rate**: Количество запросов в секунду
- **Errors**: Количество failed запросов
- **Duration**: Latency запросов (через histogram или summary)

Это базовый набор метрик для любого сервиса.

**Пример для HTTP API**:
```
http_requests_total (counter) — для Rate
http_requests_failed_total (counter) — для Errors
http_request_duration_seconds (histogram) — для Duration
```

#### 2. USE Method (для ресурсов)

**USE** = Utilization, Saturation, Errors

- **Utilization**: Процент использования ресурса (CPU, memory, disk)
- **Saturation**: Насколько ресурс перегружен (queue length, wait time)
- **Errors**: Ошибки связанные с ресурсом

**Пример для базы данных**:
- Utilization: процент использования connection pool
- Saturation: количество запросов в очереди
- Errors: количество connection timeouts

#### 3. Правильный выбор metric type

| Что измеряешь | Тип метрики | Пример |
|---------------|-------------|---------|
| Количество событий | Counter | Запросы, ошибки, сообщения |
| Текущее состояние | Gauge | Память, CPU, active connections |
| Распределение значений | Histogram | Latency, размер запроса |
| Уже вычисленные percentiles | Summary | Pre-calculated p99 |

#### 4. Naming conventions (Соглашения об именовании)

Хорошие имена метрик помогают понимать их семантику:

**Структура имени**:
```
<namespace>_<subsystem>_<name>_<unit>_<type>
```

**Примеры**:
- `http_request_duration_seconds` (duration в секундах)
- `http_requests_total` (counter, поэтому _total)
- `database_connections_active` (gauge, текущее количество)
- `cache_hits_total` (counter событий)

**Правила**:
- Counter всегда заканчивается на `_total`
- Указывай единицы измерения: `_seconds`, `_bytes`, `_ratio`
- Используй snake_case
- Имя должно быть понятным без контекста

#### 5. Labels vs separate metrics

**Используй labels** когда:
- Значения ограничены (bounded) и предсказуемы
- Нужна group-by агрегация
- Это измерение одной и той же метрики

**Пример**: HTTP статус коды
```
http_requests_total{status="200"} = 10000
http_requests_total{status="404"} = 150
http_requests_total{status="500"} = 5
```

**Используй отдельные метрики** когда:
- Семантика разная
- Разные единицы измерения
- Не нужна совместная агрегация

**Пример**: Отдельные метрики для CPU и memory
```
process_cpu_usage_percent = 45.2
process_memory_usage_bytes = 1073741824
```

#### 6. Retention и aggregation

Метрики занимают много места. Нужна стратегия retention (хранения):

**Подход с downsampling**:
- Recent data (недавние данные): храни с высоким разрешением (каждые 15 секунд) на 7 дней
- Medium-term data (среднесрочные): агрегируй до 1 минуты, храни 30 дней
- Long-term data (долгосрочные): агрегируй до 1 часа, храни 1 год

**Пример**: ClickHouse для метрик — использует materialized views для агрегации старых данных.

#### 7. Alerting на основе метрик

**Что НЕ делать**:
- Алерты на instantaneous values (мгновенные значения): `cpu_usage > 80%`
- Алерты на средние значения за большой период: `avg_over_time(5h)`

**Что делать**:
- Алерты на sustained conditions (устойчивые состояния): `cpu_usage > 80% for 5m`
- Алерты на rate of change (скорость изменения): `error_rate increased by 200%`
- Алерты на percentiles: `p99_latency > 1s for 2m`

**Почему**: Кратковременные spike это нормально. Проблема — это устойчивое ухудшение или резкое изменение тренда.

#### 8. Multi-dimensional metrics

Добавляй dimensions (измерения) через labels для гибкого анализа:

**Пример**: HTTP метрики
```
http_requests_total{
  method="GET",
  endpoint="/api/users",
  status="200",
  datacenter="us-east-1",
  version="v2.3.1"
}
```

Теперь можешь анализировать:
- По методу: сколько GET vs POST?
- По endpoint: какие endpoint самые нагруженные?
- По статусу: какой процент ошибок?
- По datacenter: есть ли проблемы в конкретном DC?
- По версии: как новая версия влияет на метрики?

**Но помни о cardinality**: Не добавляй labels с unbounded значениями!

---

## Tracing Semantics (Семантика трассировки)

### Что такое Tracing Semantics

**Tracing Semantics** (семантика трассировки) определяет правила о том, как отслеживается путь запроса через распределенную систему. Это ответы на вопросы:
- Как связываются события из разных сервисов в единый trace?
- Насколько полный trace ты получишь?
- Что происходит, если часть trace потерялась?
- Как понять причинно-следственные связи между событиями?

Представь детектива, расследующего преступление. Он собирает улики из разных мест: показания свидетелей, записи с камер, отпечатки пальцев. Tracing — это способ связать все эти улики в одну историю. Но что если:
- Камера на одной улице не работала?
- Один свидетель дал показания с опозданием?
- Временные метки на записях не синхронизированы?

### Гарантии и ограничения трассировки

#### 🎯 Что гарантирует tracing

**Causal ordering** (Причинно-следственный порядок):
- Если событие A вызвало событие B, это будет видно в trace
- Parent-child relationships между spans сохраняются

**Correlation** (Корреляция):
- События одного запроса связаны через trace_id
- Можно восстановить путь запроса через систему

**Пример**: Пользователь делает запрос "купить товар". Trace покажет:
1. API Gateway получил запрос
2. Вызвал Auth Service для проверки токена
3. Вызвал Inventory Service для проверки наличия
4. Вызвал Payment Service для оплаты
5. Вызвал Notification Service для отправки email

Все эти шаги связаны одним trace_id, и видна вложенность (parent-child).

#### ❌ Что НЕ гарантирует tracing

**Completeness** (Полнота):
- Spans могут теряться из-за сетевых проблем
- Spans могут не отправляться из-за sampling
- Если сервис упал до отправки span, он потеряется

**Absolute timing** (Абсолютное время):
- Timestamps зависят от синхронизации часов
- Разные серверы могут иметь clock drift (расхождение часов)
- Порядок событий в trace может не соответствовать реальному

**Performance overhead** (Нулевые накладные расходы):
- Tracing добавляет latency (обычно небольшую)
- Генерация и отправка spans потребляют ресурсы

### Контекст и причинность

#### Trace context propagation (Распространение контекста трассировки)

Для связи spans из разных сервисов нужно передавать контекст. Это делается через headers в HTTP или metadata в gRPC.

**W3C Trace Context standard**:
```
traceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01
             ^  ^                                ^                ^
             |  |                                |                |
             |  trace-id                         span-id          flags
             version
```

**Пример**:
```
User Request → API Gateway → Auth Service → Database
     |              |              |            |
     trace_id: abc123
                    trace_id: abc123
                    parent_span: gateway
                                 trace_id: abc123
                                 parent_span: auth
                                             trace_id: abc123
                                             parent_span: auth
```

Каждый сервис:
1. Получает trace context из входящего запроса
2. Создает свой span с уникальным span_id
3. Устанавливает parent_span_id из входящего контекста
4. Передает trace context дальше

**Проблема**: Если хотя бы один сервис в цепочке не передает контекст, trace разрывается. Ты увидишь два несвязанных trace вместо одного.

#### Causality (Причинность)

В распределенной системе events не всегда происходят в очевидном порядке.

**Пример проблемы**:
```
Service A (timestamp: 10:30:00.100) → отправил запрос в Service B
Service B (timestamp: 10:30:00.095) → получил запрос

Как такое возможно? Запрос получен раньше, чем отправлен?
```

**Причина**: Часы на Server A отстают от часов на Server B на 10ms.

**Решение**: Использовать **happened-before relationship** вместо timestamps:
- Если событие A вызвало событие B, то A happened-before B
- Это сохраняется в parent-child связях в trace

#### Logical clocks (Логические часы)

Для определения порядка событий без зависимости от wall-clock time:

**Lamport Timestamps**:
- Каждый процесс имеет счетчик
- При каждом событии счетчик увеличивается
- При отправке сообщения счетчик отправляется вместе с ним
- При получении: счетчик = max(local, received) + 1

**Vector Clocks**:
- Каждый процесс поддерживает вектор счетчиков для всех процессов
- Позволяет определять concurrent events (одновременные события)

Это сложнее в реализации, но некоторые tracing системы используют подобные подходы.

### Проблемы и инциденты трассировки

#### Инцидент 1: Broken trace из-за missing context propagation

**Ситуация**: Микросервисная архитектура. Один из сервисов обновили, но не добавили propagation trace context.

**Что произошло**:
- Request прошел через: Frontend → API Gateway → Auth Service → User Service → Database
- Auth Service не передал trace context в User Service
- В distributed tracing system появились два trace:
  - Trace 1: Frontend → API Gateway → Auth Service (выглядит завершенным)
  - Trace 2: User Service → Database (выглядит как отдельный запрос)
- При расследовании медленного запроса невозможно понять, что User Service был частью исходного запроса

**Как избежать**:
- Использовать middleware/interceptors для автоматической propagation
- Проверять completeness traces в тестах
- Мониторить "orphan spans" (spans без parent)

#### Инцидент 2: Incomplete trace из-за high sampling rate

**Ситуация**: Система трассирует только 1% запросов (sampling ratio = 0.01) для снижения overhead.

**Что произошло**:
- Пользователь жалуется на медленный запрос
- Этот запрос попал в 99%, которые не трассируются
- В tracing system нет информации об этом запросе
- Невозможно расследовать проблему

**Усложнение**: Если у тебя head-based sampling (решение принимается в начале запроса), ты теряешь возможность увидеть редкие ошибки.

**Решение**: Tail-based sampling:
- Трассируй все запросы с минимальным overhead
- После завершения запроса решай, сохранять trace или нет
- Всегда сохраняй traces с ошибками или высокой latency

#### Инцидент 3: Misleading latency из-за clock skew

**Ситуация**: Trace показывает, что span занял отрицательное время.

**Что произошло**:
```
Service A (clock: 10:00:00.500):
  start_time: 10:00:00.500
  отправил запрос → Service B

Service B (clock: 10:00:00.300, отстает на 200ms):
  received_time: 10:00:00.300
  processing: 10ms
  end_time: 10:00:00.310

В trace:
  Service A sent request at 10:00:00.500
  Service B received at 10:00:00.300
  Time travel! Запрос получен на 200ms раньше отправки
```

**Как избежать**:
- Использовать NTP для синхронизации часов
- Учитывать clock skew при анализе
- Опираться на duration spans, а не на absolute timestamps
- Использовать logical clocks где возможно

#### Инцидент 4: Lost spans из-за backpressure

**Ситуация**: Во время пиковой нагрузки traces стали неполными.

**Что произошло**:
- Приложение генерирует spans и отправляет в tracing collector
- Collector не успевает обрабатывать поток данных
- Начинает отбрасывать spans (backpressure)
- В итоге traces содержат только часть spans
- Критичные spans (с ошибками) могут быть потеряны

**Как избежать**:
- Использовать buffering на стороне приложения
- Приоритезировать spans (сначала отправлять spans с ошибками)
- Масштабировать tracing infrastructure
- Мониторить dropped spans

### Практики проектирования трассировки

#### 1. Span structure (Структура span)

Хорошо структурированный span содержит:

**Обязательные поля**:
- `trace_id`: уникальный идентификатор trace
- `span_id`: уникальный идентификатор span
- `parent_span_id`: идентификатор родительского span
- `operation_name`: название операции (например, "HTTP GET /api/users")
- `start_time`: время начала
- `duration`: длительность

**Опциональные поля**:
- `tags`: ключ-значение метаданные (например, `http.method=GET`, `http.status_code=200`)
- `logs`: структурированные события внутри span (например, "cache miss", "retry attempt")
- `span_kind`: тип span (CLIENT, SERVER, PRODUCER, CONSUMER)

**Пример структуры**:
```
Span {
  trace_id: "a1b2c3d4e5f6",
  span_id: "span123",
  parent_span_id: "spanABC",
  operation_name: "POST /api/orders",
  start_time: "2024-01-15T10:30:00.100Z",
  duration: 245ms,
  tags: {
    "http.method": "POST",
    "http.url": "/api/orders",
    "http.status_code": 200,
    "user_id": "user-42",
    "order_id": "order-789"
  },
  logs: [
    {timestamp: "10:30:00.120Z", event: "inventory_checked"},
    {timestamp: "10:30:00.200Z", event: "payment_processed"},
    {timestamp: "10:30:00.340Z", event: "notification_sent"}
  ]
}
```

#### 2. Span granularity (Гранулярность span)

Слишком детальные spans → overhead и шум.
Слишком крупные spans → недостаточно информации.

**Правило**: Создавай span для каждого "значимого" действия:
- Внешние вызовы (HTTP, gRPC, database queries)
- Значимая бизнес-логика (обработка заказа, валидация)
- Асинхронные операции (отправка в очередь, обработка сообщения)

**НЕ создавай span для**:
- Внутренних функций (математические вычисления, string operations)
- Очень быстрых операций (< 1ms)
- Высокочастотных операций в цикле

**Пример**:

Хорошо:
```
Trace: Process Order
  ├─ Span: Validate Order (15ms)
  ├─ Span: Check Inventory [call to Inventory Service] (120ms)
  ├─ Span: Process Payment [call to Payment Service] (450ms)
  └─ Span: Send Notification [call to Notification Service] (80ms)
```

Плохо (слишком детально):
```
Trace: Process Order
  ├─ Span: Parse JSON (0.5ms)
  ├─ Span: Validate field 'customer_id' (0.1ms)
  ├─ Span: Validate field 'amount' (0.1ms)
  ├─ Span: Create HTTP client (0.3ms)
  ├─ Span: Serialize request (1ms)
  ├─ Span: Send HTTP request (120ms)
  ├─ Span: Deserialize response (0.8ms)
  ... (сотни spans)
```

#### 3. Context propagation patterns

**In-process propagation**:
- Используй thread-local storage или context objects
- В Go: `context.Context`
- В Java: ThreadLocal
- В Python: contextvars

**Cross-process propagation**:
- HTTP headers: `traceparent`, `tracestate` (W3C standard)
- gRPC metadata
- Message headers в Kafka/RabbitMQ

**Asynchronous propagation**:
- При async operations сохраняй context
- Передавай context в callback/future

**Пример проблемы**:
```
// НЕправильно
async function processOrder(orderId) {
  // Новый trace context!
  const order = await getOrder(orderId);
  // Parent trace потерян
}

// Правильно
async function processOrder(orderId, traceContext) {
  // Используем существующий context
  const order = await getOrder(orderId, traceContext);
  // Parent trace сохранен
}
```

#### 4. Tagging strategy (Стратегия тегирования)

Tags позволяют фильтровать и группировать traces.

**Обязательные tags**:
- `error=true` если операция завершилась ошибкой
- `http.status_code` для HTTP requests
- `db.statement` для database queries (но будь осторожен с PII!)
- `messaging.destination` для message queues

**Полезные tags**:
- `user_id` или `session_id` (если не содержат PII)
- `component` (название библиотеки/фреймворка)
- `version` (версия приложения)
- `datacenter` или `region`

**Не используй в tags**:
- PII (Personally Identifiable Information): email, phone, полное имя
- Secrets: passwords, tokens, API keys
- High cardinality data: полные URLs с query params, SQL queries с literals

**Почему**: Tags индексируются. High cardinality увеличивает размер индекса и замедляет поиск.

#### 5. Sampling strategies (Стратегии сэмплирования)

Трассировка всех запросов дорого. Нужно выбирать.

**Head-based sampling** (Решение в начале):
- Решение принимается при создании trace
- Простая реализация
- Проблема: можешь пропустить интересные traces (ошибки, медленные запросы)

**Tail-based sampling** (Решение в конце):
- Решение принимается после завершения trace
- Можно выбирать на основе характеристик (latency, errors)
- Сложнее реализовать (нужен collector, который видит весь trace)

**Adaptive sampling** (Адаптивное):
- Динамически изменяет sampling rate
- При высокой нагрузке — меньше traces
- При ошибках — больше traces

**Stratified sampling** (Стратифицированное):
- Разные sampling rates для разных типов запросов
- Например:
  - `/health-check`: 0.1% (очень мало)
  - `/api/*`: 1% (нормально)
  - Requests с errors: 100% (все)

#### 6. Error tracking в traces

Когда происходит ошибка, span должен это отражать:

**Обязательно**:
- Установить тег `error=true`
- Добавить тег `error.type` (класс исключения)
- Добавить тег `error.message` (сообщение об ошибке)

**Опционально**:
- Добавить log event с stack trace
- Добавить контекст (что пытались сделать, с какими параметрами)

**Пример**:
```
Span {
  operation_name: "UPDATE users SET ...",
  tags: {
    "error": true,
    "error.type": "DatabaseConnectionTimeout",
    "error.message": "Connection to PostgreSQL timed out after 30s",
    "db.host": "postgres-primary.example.com",
    "db.port": 5432
  },
  logs: [
    {
      timestamp: "...",
      event: "error",
      message: "Failed to acquire connection from pool",
      stack_trace: "..."
    }
  ]
}
```

#### 7. Correlation с logs и metrics

Traces, logs и metrics должны быть связаны:

**Добавляй в logs**:
- `trace_id` и `span_id` в каждую log entry
- Позволяет по log entry найти соответствующий trace

**Добавляй в metrics**:
- Tags, соответствующие trace tags
- Позволяет сравнивать trends в метриках с конкретными traces

**Workflow**:
1. Видишь на dashboard метрику: "error rate increased"
2. Смотришь traces с ошибками за этот период
3. Выбираешь репрезентативный trace
4. Смотришь logs для этого trace_id
5. Получаешь полную картину

#### 8. Distributed tracing в асинхронных системах

В event-driven архитектурах trace может распространяться через очереди.

**Пример**: Order Service → Kafka → Payment Service

```
Order Service:
  1. Создает span "publish_order_event"
  2. Сериализует trace context в message headers
  3. Публикует message в Kafka

Kafka:
  - Просто хранит message с headers

Payment Service:
  1. Читает message из Kafka
  2. Извлекает trace context из headers
  3. Создает span "process_order_event" с parent = span из Order Service
```

**Проблема**: Если между publish и consume проходит много времени, trace может выглядеть странно (большая "дыра" во времени).

**Решение**: Добавляй timestamps в message:
- `published_at`: когда message был опубликован
- `consumed_at`: когда message был прочитан
- `queue_time = consumed_at - published_at`

---

## Event Ordering Semantics (Семантика упорядочивания событий)

### Что такое Event Ordering Semantics

**Event Ordering Semantics** (семантика упорядочивания событий) определяет правила о том, в каком порядке события наблюдаются и записываются в системе observability. Это ответы на вопросы:
- Соответствует ли порядок событий в логах реальному порядку?
- Можно ли восстановить причинно-следственные связи из порядка событий?
- Что происходит, когда события из разных источников смешиваются?
- Как интерпретировать timestamps в распределенной системе?

Представь концерт с несколькими камерами. Потом монтажер склеивает видео. Если камеры не синхронизированы, на видео может получиться, что гитарист сыграл ноту раньше, чем барабанщик ударил, хотя на самом деле было наоборот. Event ordering — это правила о том, как "склеивать" события из разных источников.

### Гарантии и ограничения упорядочивания

#### 🎯 Что может гарантировать система

**Total order в одном источнике**:
- События от одного процесса/потока обычно упорядочены
- Порядок записи в log file = порядок генерации (если одна нить)

**Partial order в распределенной системе**:
- Можно установить причинно-следственные связи (happened-before)
- Но не всегда можно сказать, какое из двух независимых событий было раньше

**Пример**:
```
Service A: Event 1 (10:00:00.100)
Service B: Event 2 (10:00:00.105)

Был ли Event 1 раньше Event 2?
Зависит от синхронизации часов!
Если часы Service A опережают на 10ms, то на самом деле Event 2 был раньше.
```

#### ❌ Что НЕ гарантируется

**Global total order** (Глобальный полный порядок):
- В распределенной системе невозможно без координации
- Timestamps с разных серверов могут не совпадать
- Network latency влияет на порядок доставки

**Real-time ordering** (Упорядочивание в реальном времени):
- Порядок arrival (прибытия) ≠ порядок occurrence (возникновения)
- События могут приходить в log aggregator не в том порядке

**Consistency между разными observability системами**:
- Logs могут показывать один порядок
- Traces — другой
- Metrics — третий (агрегированный)

### Типы упорядочивания

#### 1. Timestamp ordering (Упорядочивание по временной метке)

Самый простой подход: сортируй по timestamp.

**Wall-clock time ordering**:
- Используй system time каждого сервера
- Просто реализовать
- Проблема: clock skew между серверами

**Пример проблемы**:
```
Server A (clock +5 seconds ahead):
  [10:00:05.000] User clicked button

Server B (correct time):
  [10:00:00.500] Button click event received

В aggregated logs:
  [10:00:00.500] Button click event received  ← кажется, что это раньше
  [10:00:05.000] User clicked button          ← но на самом деле это было раньше!
```

**NTP (Network Time Protocol)**:
- Синхронизирует часы серверов
- Точность обычно в пределах 1-10ms
- Но не гарантирует идеальную синхронизацию

**Hybrid logical clocks**:
- Комбинация physical time и logical counter
- Увеличивает counter при clock drift
- Используется в некоторых распределенных БД (например, CockroachDB)

#### 2. Causal ordering (Причинно-следственное упорядочивание)

Упорядочивание на основе happened-before relationship.

**Определение happened-before (A → B)**:
1. A и B в одном процессе, и A случилось раньше B по программному порядку
2. A — это отправка сообщения, B — получение этого сообщения
3. Транзитивность: если A → B и B → C, то A → C

**Concurrent events** (Одновременные события):
- События A и B concurrent, если ни A → B, ни B → A
- Невозможно определить, какое было раньше

**Пример**:
```
Process 1:        Process 2:
  A1: Send req      B1: Recv req
       ↓               ↓
  A2: Log sent     B2: Process req
                       ↓
                    B3: Send resp
                       ↓
  A3: Recv resp

Happened-before relationships:
  A1 → B1 (send → receive)
  B1 → B2 (в одном процессе)
  B2 → B3 (в одном процессе)
  B3 → A3 (send → receive)
  A1 → A2 (в одном процессе)

Concurrent:
  A2 и B2 concurrent (не связаны happened-before)
```

#### 3. Arrival ordering (Упорядочивание по прибытию)

Порядок, в котором события прибыли в систему наблюдаемости.

**Характеристики**:
- Легко реализовать
- Не требует синхронизации часов
- Но может сильно отличаться от реального порядка

**Пример**: Логи из Nginx через Filebeat в Elasticsearch

```
Real order:
  t0: Request 1 arrives at Nginx
  t1: Request 2 arrives at Nginx
  t2: Request 1 processed (slow query)
  t3: Request 2 processed (fast from cache)

Arrival order в Elasticsearch:
  t10: Request 2 log arrives (written first, because processed first)
  t11: Request 1 log arrives

В Elasticsearch видишь: Request 2 → Request 1
Но на самом деле: Request 1 → Request 2
```

**Когда использовать**:
- Для real-time monitoring (важна свежесть данных)
- Когда точный порядок не критичен
- Для event streaming

#### 4. Sequence number ordering (Упорядочивание по номеру последовательности)

Каждое событие получает монотонно возрастающий номер.

**В одном процессе**:
```
Event 1: seq=1
Event 2: seq=2
Event 3: seq=3
```

Простая и надежная схема.

**В распределенной системе**:
- Нужен координатор для выдачи sequence numbers
- Или vector clocks (каждый процесс имеет свой счетчик)

**Пример**: Kafka partitions
- Каждое сообщение в partition получает offset (по сути, sequence number)
- Внутри одного partition — total order
- Между разными partitions — partial order

**Преимущества**:
- Не зависит от clock synchronization
- Легко определить порядок
- Можно детектировать пропущенные события (gap в sequence)

**Недостатки**:
- В распределенной системе нужна координация (bottleneck)
- Или приходится мириться с partial order (как в Kafka)

#### 5. FIFO ordering (Упорядочивание "первым пришел — первым вышел")

Гарантия: сообщения от одного sender к одному receiver приходят в порядке отправки.

**Пример**: TCP connection
- TCP гарантирует FIFO порядок байтов
- Но только в рамках одного соединения

**В observability**:
- FIFO между приложением и log shipper
- Но не гарантирует FIFO в end-to-end pipeline

```
Application → (FIFO) → Log shipper → (не FIFO) → Log aggregator

Если log shipper отправляет логи батчами через network, порядок батчей может перемешаться.
```

### Проблемы и инциденты упорядочивания

#### Инцидент 1: Неверная последовательность в логах из-за clock skew

**Ситуация**: Распределенное приложение на 50 серверах. Логи из всех серверов собираются в Elasticsearch.

**Что произошло**:
- На одном сервере часы спешили на 30 секунд
- В логах этот сервер "из будущего"
- При поиске по времени логи перемешаны
- Невозможно понять реальную последовательность событий

**Конкретный сценарий**:
```
11:00:00 [Server A] Request started
11:00:15 [Server B] Called downstream service  ← часы на 30 сек впереди (реально 10:59:45)
11:00:05 [Server A] Got response
11:00:20 [Server B] Response sent

В логах выглядит так:
  Server B вызвал downstream service ДО того, как Server A начал request
  И даже ДО того, как Server B сам получил response!
```

**Как избежать**:
- Регулярно проверять clock drift между серверами
- Настроить алерты на большой drift (> 1 секунда)
- Использовать NTP daemon на всех серверах
- В критичных случаях использовать logical clocks

#### Инцидент 2: Lost causality из-за async logging

**Ситуация**: Приложение использует асинхронное логирование для performance.

**Что произошло**:
```
Thread 1:
  Write to database: INSERT user (id=42)
  Log: "User created: id=42"  ← помещен в async buffer

Thread 2:
  Read from database: SELECT user WHERE id=42
  Log: "User found: id=42"    ← помещен в async buffer

Async logger flush:
  Сначала записал лог из Thread 2 (был раньше в buffer)
  Потом лог из Thread 1

В логах:
  "User found: id=42"      ← как нашли то, что еще не создано?
  "User created: id=42"
```

**Причина**: Асинхронный logger не гарантирует порядок flush для разных threads.

**Как избежать**:
- Использовать thread-safe buffering с сохранением порядка
- Добавлять sequence numbers в логи
- Добавлять timestamp с высокой точностью (nanoseconds)
- Для критичных событий использовать синхронное логирование

#### Инцидент 3: Reordering в Kafka consumers

**Ситуация**: Event-driven система. Events публикуются в Kafka, consumer их обрабатывает.

**Что произошло**:
- События публикуются в Kafka с разными keys
- Kafka гарантирует порядок только внутри одного partition
- События с разными keys попадают в разные partitions
- Consumer читает из multiple partitions одновременно
- События обрабатываются в непредсказуемом порядке

**Конкретный пример**:
```
Event 1: User registered (user_id=42)      → Partition 1
Event 2: Order created (order_id=100)      → Partition 2
Event 3: User verified email (user_id=42)  → Partition 1

Consumer может прочитать в порядке:
  Event 2 (из Partition 2)
  Event 1 (из Partition 1)
  Event 3 (из Partition 1)

Если Event 2 зависит от Event 1, возникнет ошибка: "Order for non-existent user"
```

**Как избежать**:
- Использовать partition key правильно (связанные события — один key)
- Если нужен global order — использовать single partition (но это bottleneck)
- Делать операции идемпотентными
- Использовать state store для проверки зависимостей

#### Инцидент 4: Out-of-order traces в distributed tracing

**Ситуация**: Microservices с distributed tracing.

**Что произошло**:
```
Service A → Service B → Service C

Service A:
  Span A (start: 10:00:00.100, duration: 50ms)
    → call Service B at 10:00:00.110

Service B:
  Span B (start: 10:00:00.105, duration: 30ms)  ← часы отстают на 10ms
    → call Service C at 10:00:00.120 (реально 10:00:00.130)

Service C:
  Span C (start: 10:00:00.115, duration: 10ms)  ← часы отстают на 20ms (реально 10:00:00.135)

В trace view:
  Span A: [===========================] 10:00:00.100 - 10:00:00.150
    Span B: [===============] 10:00:00.105 - 10:00:00.135  ← начался ДО вызова?
      Span C: [====] 10:00:00.115 - 10:00:00.125          ← закончился ДО вызова?

Визуально это выглядит странно и вводит в заблуждение.
```

**Как избежать**:
- Синхронизировать часы через NTP
- В trace viewer опираться на parent-child relationships, а не только на timestamps
- Использовать logical duration вместо wall-clock timestamps
- Добавлять информацию о clock skew в spans

### Практики проектирования упорядочивания

#### 1. Добавление ordering metadata

В каждое событие добавляй информацию для восстановления порядка:

**Обязательные поля**:
- `timestamp`: wall-clock time (ISO 8601 с microseconds)
- `sequence_number`: монотонно возрастающий счетчик в пределах источника
- `source_id`: идентификатор источника (hostname, service name)

**Опциональные поля**:
- `causality_id`: идентификатор причинного события (parent event id)
- `session_id` или `trace_id`: для группировки связанных событий
- `process_id`, `thread_id`: для точного упорядочивания в рамках одного хоста

**Пример лога**:
```json
{
  "timestamp": "2024-01-15T10:00:00.123456Z",
  "sequence_number": 42857,
  "source_id": "api-server-03",
  "thread_id": 12345,
  "trace_id": "abc123",
  "message": "Request processed",
  "request_id": "req-789"
}
```

#### 2. Использование monotonic clocks для duration

Для измерения duration (длительности) используй monotonic clock, а не wall-clock.

**Wall-clock проблемы**:
- Может "прыгать" при NTP sync
- Может идти назад при ручной корректировке

**Monotonic clock**:
- Всегда монотонно увеличивается
- Не подвержен NTP adjustments
- Идеален для измерения elapsed time

**Пример**:
```
// Плохо
start_time = wall_clock.now()
... операция ...
end_time = wall_clock.now()
duration = end_time - start_time  ← может быть отрицательным!

// Хорошо
start = monotonic_clock.now()
... операция ...
end = monotonic_clock.now()
duration = end - start  ← всегда >= 0
```

В логи пиши и то, и другое:
- `timestamp`: wall-clock для корреляции с внешними событиями
- `duration`: monotonic для точного измерения

#### 3. Consistent hashing для партиционирования

Если используешь партиционирование (например, Kafka), правильно выбирай partition key:

**Правило**: События, для которых важен порядок, должны идти в один partition.

**Примеры**:

**E-commerce**:
- Key = `user_id`: все события пользователя упорядочены
  - User registered
  - User added item to cart
  - User completed order

**IoT**:
- Key = `device_id`: все события устройства упорядочены
  - Device connected
  - Sensor reading
  - Device disconnected

**Проблема**: Если key имеет high cardinality (много уникальных значений), получишь много partitions. Но порядок внутри каждого будет сохранен.

#### 4. Vector clocks для causal ordering

Для сложных distributed систем, где важна причинность:

**Идея**:
- Каждый процесс P имеет вектор счетчиков: VC[P]
- VC[P][P] = логические часы процесса P
- VC[P][Q] = последнее известное состояние часов процесса Q

**Правила**:
1. Перед локальным событием: VC[P][P]++
2. При отправке сообщения: включи VC[P] в сообщение
3. При получении сообщения с VC[Q]: VC[P][i] = max(VC[P][i], VC[Q][i]) for all i, затем VC[P][P]++

**Сравнение событий**:
- Event A < Event B если VC[A][i] ≤ VC[B][i] для всех i, и хотя бы для одного строгое неравенство
- Иначе события concurrent

Это позволяет определить happened-before без синхронизации часов.

**Проблема**: Vector clocks требуют O(N) памяти, где N = количество процессов. Для больших систем это дорого.

#### 5. Windowing для late-arriving events

В stream processing часто события приходят с задержкой.

**Event time vs Processing time**:
- Event time: когда событие произошло
- Processing time: когда событие обработано

**Пример**:
```
Event 1: created at 10:00:00, processed at 10:00:05  → lag 5s
Event 2: created at 10:00:01, processed at 10:00:10  → lag 9s (пришло позже Event 1, но создано раньше)
```

**Windowing strategies**:

**Tumbling window**:
- Фиксированные неперекрывающиеся интервалы
- Пример: [10:00:00-10:00:10), [10:00:10-10:00:20), ...

**Sliding window**:
- Перекрывающиеся интервалы
- Пример: каждую секунду — window за последние 10 секунд

**Session window**:
- Динамические окна на основе activity
- Закрывается после timeout неактивности

**Watermarks**:
- Оценка "мы получили все события до времени T"
- Позволяет закрыть window и вычислить агрегаты
- Но поздние события (после watermark) могут быть потеряны или обработаны отдельно

#### 6. Idempotency для защиты от reordering

Если порядок не гарантирован, делай операции идемпотентными:

**Идемпотентность**: Выполнение операции несколько раз = выполнение один раз.

**Примеры**:

**Неидемпотентная операция**:
```
UPDATE account SET balance = balance + 100 WHERE id = 42
```
Если выполнить дважды, баланс увеличится на 200.

**Идемпотентная операция**:
```
IF transaction_id not in processed_transactions:
  UPDATE account SET balance = balance + 100 WHERE id = 42
  INSERT INTO processed_transactions (transaction_id, ...)
```

Теперь можно безопасно retry.

**Для observability**:
- Логи с уникальным ID можно дедуплицировать
- Метрики с timestamp можно сравнивать (не перезаписывать старое новым)
- Трейсы с уникальным trace_id можно мержить

#### 7. Separate collection и presentation order

**Collection order**: порядок сбора событий (обычно arrival order).
**Presentation order**: порядок отображения (обычно timestamp или causal order).

Не путай их!

**В log aggregator**:
1. Собирай логи как можно быстрее (arrival order)
2. Индексируй по timestamp
3. Отображай в правильном порядке

**Пример**: Elasticsearch
- Принимает documents в любом порядке
- Индексирует по `@timestamp`
- Query возвращает в порядке timestamp
- Но нужно учитывать clock skew!

**Best practice**: Добавляй `ingestion_time` (время попадания в систему) отдельно от `event_time` (когда событие произошло). Это помогает детектировать late-arriving events.

---

## Sampling Semantics (Семантика сэмплирования)

### Что такое Sampling Semantics

**Sampling Semantics** (семантика сэмплирования) определяет правила о том, какие данные собираются, а какие отбрасываются в системе observability. Это ответы на вопросы:
- Какой процент данных ты видишь?
- Как выбираются данные для сбора?
- Какие данные могут быть потеряны при sampling?
- Можно ли доверять статистике на основе sample?

Представь, что ты делаешь социологический опрос. Опросить всех жителей города невозможно — слишком дорого. Ты опрашиваешь sample (выборку). Но:
- Как выбрать людей для опроса? (random, stratified, etc.)
- Сколько людей опросить? (sample size)
- Можно ли обобщить результаты на весь город? (statistical significance)
- Что если выборка смещенная? (selection bias)

Sampling в observability работает аналогично.

### Гарантии и ограничения сэмплирования

#### 🎯 Что может гарантировать sampling

**Statistical representativeness** (при правильной реализации):
- Sample отражает общее распределение
- Можно экстраполировать метрики

**Reduced overhead**:
- Меньше данных = меньше ресурсов
- Меньше влияние на production performance

**Пример**: Вместо трассировки 100% запросов (1 миллион трейсов в минуту), трассируешь 1% (10 тысяч трейсов). Экономия хранилища в 100 раз, но все еще достаточно данных для анализа.

#### ❌ Что НЕ гарантирует sampling

**Полнота данных**:
- Редкие события могут быть пропущены
- Конкретный проблемный запрос может не попасть в sample

**Точность метрик**:
- Всегда есть statistical error
- Чем меньше sample size, тем больше погрешность

**Воспроизводимость**:
- При random sampling каждый раз получишь немного разные результаты

**Пример проблемы**: У тебя 1000 запросов в секунду, из них 10 с ошибками (1%). При sampling 1% в среднем увидишь 0.1 ошибки в секунду. Но:
- В одну секунду можешь увидеть 0 ошибок
- В другую — 1 ошибку
- Невозможно точно определить момент начала проблемы

### Типы и стратегии сэмплирования

#### 1. Fixed-rate sampling (Фиксированная частота)

Самая простая стратегия: собирай N% всех событий.

**Реализация**:
- Генерируй random число от 0 до 1
- Если число < sampling_rate, собирай событие
- Иначе отбрасывай

**Пример**: Sampling rate = 10%
```
Request 1: random() = 0.05 < 0.10 → Sample ✓
Request 2: random() = 0.42 > 0.10 → Drop ✗
Request 3: random() = 0.08 < 0.10 → Sample ✓
Request 4: random() = 0.91 > 0.10 → Drop ✗
```

**Преимущества**:
- Простота реализации
- Предсказуемый объем данных
- Unbiased (несмещенный) при правильной реализации

**Недостатки**:
- Редкие события (ошибки) могут быть пропущены
- Не адаптируется к изменению нагрузки

**Когда использовать**:
- Для high-traffic систем с равномерным распределением
- Когда все запросы примерно одинаково важны
- Для базового observability

#### 2. Adaptive sampling (Адаптивное сэмплирование)

Динамически изменяй sampling rate в зависимости от нагрузки.

**Идея**:
- При низкой нагрузке: высокий sampling rate (можем себе позволить)
- При высокой нагрузке: низкий sampling rate (чтобы не перегрузить систему)

**Пример**:
```
Load < 100 RPS:    sampling_rate = 100% (все трейсы)
Load 100-1000 RPS: sampling_rate = 10%
Load 1000-10k RPS: sampling_rate = 1%
Load > 10k RPS:    sampling_rate = 0.1%
```

**Реализация**:
- Измеряй текущую нагрузку (RPS)
- Вычисляй target sample count (например, 100 трейсов в секунду)
- Sampling rate = target / current_load

**Преимущества**:
- Постоянный объем данных независимо от нагрузки
- Не перегружает систему при spike

**Недостатки**:
- Более сложная реализация
- При очень высокой нагрузке можешь пропустить проблемы
- Нужно правильно выбрать target sample count

#### 3. Probabilistic sampling (Вероятностное сэмплирование)

Принятие решения о sampling на основе вероятности с учетом характеристик события.

**Consistent sampling**:
- На основе hash от уникального ID (например, user_id, request_id)
- Один и тот же ID всегда получает одно решение
- Полезно для distributed tracing (все spans одного trace либо все собираются, либо все отбрасываются)

**Пример**:
```
trace_id = "a1b2c3d4e5f6"
hash = hash_function(trace_id) % 100
sampling_rate = 10%

if hash < 10:
  sample all spans of this trace
else:
  drop all spans of this trace
```

**Преимущества**:
- Consistent решение для связанных событий
- Не нужна координация между сервисами
- Хорошо работает с distributed tracing

**Недостатки**:
- Если hash распределен неравномерно, получишь bias
- Все еще может пропустить редкие события

#### 4. Priority sampling (Приоритетное сэмплирование)

Выбирай "важные" события с большей вероятностью.

**Критерии важности**:
- Наличие ошибок (error = true)
- Высокая latency (duration > threshold)
- Редкие endpoints (low traffic endpoints)
- Specific users (VIP users, internal testing)

**Пример стратегии**:
```
if error:
  sampling_rate = 100%  // все ошибки
elif latency > 1s:
  sampling_rate = 50%   // половина медленных запросов
elif endpoint == "/health":
  sampling_rate = 0.1%  // почти не логируем health checks
else:
  sampling_rate = 1%    // обычные запросы
```

**Преимущества**:
- Не пропускаешь важные события (ошибки)
- Эффективно используешь budget на sampling
- Лучше для troubleshooting

**Недостатки**:
- Biased sample (не репрезентативен)
- Сложнее экстраполировать метрики
- Нужно заранее определить критерии важности

#### 5. Stratified sampling (Стратифицированное сэмплирование)

Разбивай события на strata (слои) и применяй разный sampling к каждому слою.

**Идея**: Разные типы запросов имеют разные характеристики и важность.

**Пример**:

| Stratum | Характеристика | Traffic | Sampling rate | Sample count |
|---------|---------------|---------|---------------|--------------|
| Health checks | `/health`, `/ready` | 10,000 RPS | 0.01% | 1/sec |
| Read API | `GET /api/*` | 5,000 RPS | 1% | 50/sec |
| Write API | `POST /api/*`, `PUT /api/*` | 500 RPS | 10% | 50/sec |
| Admin API | `/admin/*` | 10 RPS | 100% | 10/sec |
| Errors | `status >= 400` | 50 RPS | 100% | 50/sec |

**Преимущества**:
- Каждый stratum получает достаточно samples
- Низкочастотные, но важные события не теряются
- Можно анализировать каждый stratum отдельно

**Недостатки**:
- Сложнее реализовать
- Нужно заранее определить strata
- Сложнее вычислять общие метрики (нужен weighted average)

#### 6. Reservoir sampling (Сэмплирование резервуаром)

Выбирай fixed-size sample из stream неизвестного размера.

**Алгоритм**:
1. Заполни reservoir первыми k элементами
2. Для n-го элемента (n > k): с вероятностью k/n замени случайный элемент в reservoir

**Пример**: Хочешь собрать ровно 100 трейсов из неизвестного количества.
```
Reservoir size = 100

First 100 traces: добавить в reservoir

Trace 101: с вероятностью 100/101 заменить случайный trace в reservoir
Trace 102: с вероятностью 100/102 заменить случайный trace в reservoir
...
Trace 10000: с вероятностью 100/10000 заменить случайный trace в reservoir

В итоге: reservoir содержит 100 равновероятных traces из всех 10000
```

**Преимущества**:
- Фиксированный объем памяти
- Каждый элемент имеет равную вероятность попасть в sample
- Работает со streams

**Недостатки**:
- Нужно знать total count для корректных весов
- Не работает для distributed систем (нужна централизация)
- Не подходит для real-time (нужно дождаться конца stream)

#### 7. Tail-based sampling (Сэмплирование на основе конца)

Решение о sampling принимается ПОСЛЕ завершения trace, на основе его характеристик.

**Head-based vs Tail-based**:
- Head-based: решение в начале (при создании trace)
- Tail-based: решение в конце (после завершения trace)

**Workflow tail-based sampling**:
1. Собирай все spans с минимальным overhead (in-memory buffer)
2. Когда trace завершен, анализируй его характеристики:
   - Duration
   - Наличие ошибок
   - Количество spans
   - Затронутые сервисы
3. Решай: сохранить или отбросить
4. Если сохранить — отправь в storage, иначе удали из buffer

**Пример критериев**:
```
Keep trace if:
  - has errors
  - duration > p99
  - touches rare service
  - random(1%) // всегда храни 1% для baseline

Otherwise: drop
```

**Преимущества**:
- Не теряешь важные traces (с ошибками, медленные)
- Более эффективно используешь storage
- Лучше для troubleshooting

**Недостатки**:
- Сложнее реализовать (нужен collector, который видит весь trace)
- Увеличенная latency (нужно ждать завершения trace)
- Требует больше памяти (buffer для всех traces)
- В distributed системе нужна координация (все spans одного trace должны попасть к одному collector)

#### 8. Dynamic sampling на основе ML

Используй machine learning для предсказания "интересности" события.

**Идея**: Обучи модель определять, какие traces вероятно будут полезны для debugging.

**Features для ML модели**:
- Endpoint
- User segment
- Time of day
- Recent error rate
- Historical latency distribution

**Workflow**:
1. Собирай baseline dataset с равномерным sampling
2. Label traces: "useful" (привели к обнаружению issues) vs "not useful"
3. Обучи модель предсказывать usefulness
4. В production: model предсказывает score, sample с вероятностью proportional to score

**Преимущества**:
- Адаптируется к паттернам твоей системы
- Может обнаружить неочевидные корреляции
- Максимально эффективное использование sampling budget

**Недостатки**:
- Очень сложно реализовать и поддерживать
- Требует labeled dataset
- Может пропустить novel types of issues (модель не знает о новых проблемах)
- Overhead на inference

### Проблемы и инциденты сэмплирования

#### Инцидент 1: Пропущенная критическая ошибка из-за aggressive sampling

**Ситуация**: Высоконагруженный API с sampling rate = 0.1% (1 из 1000 запросов).

**Что произошло**:
- Новый bug приводил к ошибке в ~0.5% запросов
- При 100,000 RPS это 500 ошибок в секунду
- Но с sampling 0.1% видно только 0.5 ошибки в секунду
- Алерты не сработали (threshold был на 1 ошибку/сек)
- Пользователи массово жаловались, но monitoring не показывал проблему

**Почему произошло**:
- Sampling rate был одинаков для успешных и неуспешных запросов
- Редкие ошибки "растворились" в small sample

**Как избежать**:
- Использовать priority sampling: sample 100% errors
- Отдельный tracking для error rate (без sampling)
- Алерты на основе unsample метрик (хотя бы counter)

#### Инцидент 2: Broken trace из-за inconsistent sampling

**Ситуация**: Microservices с distributed tracing. Каждый сервис независимо принимает решение о sampling.

**Что произошло**:
```
User request → Service A (sampled: YES) → Service B (sampled: NO) → Service C (sampled: YES)

В tracing system:
  Span A: API Gateway
    (missing child)
  Span C: Database query (appears as orphan, no parent)
```

- Trace разорван
- Невозможно понять полный путь запроса
- Span C выглядит как отдельный, несвязанный запрос

**Причина**: Каждый сервис делал independent sampling decision.

**Как избежать**:
- Head-based sampling: решение принимается на входе и propagates вниз
- Все сервисы уважают решение первого сервиса
- Передавай sampling decision в trace context

#### Инцидент 3: Biased sample привел к неверным метрикам

**Ситуация**: E-commerce сайт с sampling, основанным на hash(user_id).

**Что произошло**:
```
sampling_decision = hash(user_id) % 100 < sampling_rate * 100
```

- Проблема была в том, что новые пользователи (с ID > 1,000,000) имели другое распределение hash
- Sample оказался смещен в сторону старых пользователей
- Метрики показывали, что conversion rate стабилен
- Но на самом деле новые пользователи имели гораздо более низкий conversion rate
- Проблему обнаружили только через A/B тест на full population

**Причина**: Hash function был неравномерным для разных диапазонов user_id.

**Как избежать**:
- Использовать cryptographic hash functions (равномерное распределение)
- Проверять distribution sample vs population
- Сравнивать sampled metrics с unsampled counters

#### Инцидент 4: Memory overflow при tail-based sampling

**Ситуация**: Внедрили tail-based sampling для distributed tracing.

**Что произошло**:
- Collector буферизует все spans в памяти до завершения trace
- Внезапный spike traffic (10x normal)
- Traces не завершаются быстро (из-за перегрузки downstream сервисов)
- Buffer переполнился
- Collector упал с OOM (Out Of Memory)
- Потеряли все traces за этот период

**Причина**:
- Не учли worst-case scenario: высокая нагрузка + долгие traces
- Не было fallback strategy

**Как избежать**:
- Ограничить размер buffer
- Fallback к head-based sampling при переполнении buffer
- TTL для traces в buffer (если trace не завершился за N секунд, принимай решение принудительно)
- Мониторить buffer utilization

#### Инцидент 5: Отсутствие visibility в low-traffic endpoints

**Ситуация**: Uniform sampling 1% применялся ко всем endpoints.

**Что произошло**:
- Большинство traffic: `/api/posts` (10,000 RPS) → 100 samples/sec
- Редкий endpoint: `/api/admin/delete-user` (1 request/hour) → 0.01 samples/hour = практически никогда не sample

- Критический bug в admin endpoint
- Ни одного trace для debugging
- Невозможно расследовать

**Как избежать**:
- Stratified sampling: разные rates для разных endpoints
- Минимальный sample count для каждого endpoint
- Приоритет для low-traffic но критичных endpoints

### Практики проектирования сэмплирования

#### 1. Multi-layer sampling strategy

Не используй одну стратегию для всего. Комбинируй layers:

**Layer 1: Collection layer** (на стороне приложения)
- Priority sampling: всегда sample errors и медленные запросы
- Stratified sampling: разные rates для разных endpoints
- Target: уменьшить overhead на приложение

**Layer 2: Transmission layer** (отправка в collector)
- Adaptive sampling: контролируй network bandwidth
- Batching и compression
- Target: не перегрузить сеть

**Layer 3: Storage layer** (в collector/storage)
- Tail-based sampling: финальное решение о хранении
- Downsampling старых данных
- Target: контролировать storage costs

**Пример**:
```
100,000 requests/sec
  ↓ Layer 1: Priority sampling (keep 5% + all errors)
5,000 traces/sec + 100 errors/sec
  ↓ Layer 2: Adaptive sampling (max 1000 traces/sec to collector)
1,100 traces/sec
  ↓ Layer 3: Tail-based (keep interesting traces)
500 traces/sec to storage
```

#### 2. Sample metadata и weights

Когда ты sample, добавляй metadata о sampling:

**Обязательные поля**:
- `sampled`: true/false
- `sampling_rate`: вероятность sampling (например, 0.01 = 1%)
- `sampling_weight`: 1 / sampling_rate (для экстраполяции)

**Пример**:
```json
{
  "trace_id": "abc123",
  "sampled": true,
  "sampling_rate": 0.01,
  "sampling_weight": 100,
  "spans": [...]
}
```

**Для вычисления метрик**:
```
// Сколько запросов было на самом деле?
actual_count = sampled_count * sampling_weight

// Если увидел 50 sampled requests с weight=100
actual_count = 50 * 100 = 5,000 requests
```

#### 3. Deterministic sampling для reproducibility

Для одних и тех же входных данных всегда принимай одно и то же решение.

**Используй seed для random generator**:
```
seed = hash(trace_id)
random_generator.seed(seed)
sample = random_generator.random() < sampling_rate
```

**Зачем**:
- Reproducibility: можешь "переиграть" sampling decision
- Consistency в distributed system
- Debugging: можешь проверить, почему конкретный trace был sampled или dropped

#### 4. Sample size estimation

Сколько нужно samples для статистической значимости?

**Формула** (для пропорции с заданной погрешностью):
```
n = (Z^2 * p * (1-p)) / E^2

где:
n = sample size
Z = Z-score для желаемого confidence level (1.96 для 95%)
p = предполагаемая пропорция (например, error rate)
E = margin of error (допустимая погрешность)
```

**Пример**: Хочешь оценить error rate с точностью ±0.1% (E=0.001) и 95% confidence.
Предполагаемый error rate = 1% (p=0.01).

```
n = (1.96^2 * 0.01 * 0.99) / 0.001^2
n = (3.84 * 0.0099) / 0.000001
n = 38,016

Нужно ~38,000 samples
```

Если у тебя 1,000,000 requests, то sampling rate = 38,000 / 1,000,000 = 3.8%.

#### 5. Progressive sampling для rare events

Для detection редких событий используй progressive approach:

**Идея**: Начни с high sampling rate, постепенно уменьшай.

**Workflow**:
1. Новый deploy или новая feature: sample 100% на короткое время (5-10 минут)
2. Если все хорошо: уменьши до 10% (следующие 30 минут)
3. Если все еще хорошо: уменьши до обычного rate (1%)

**Зачем**: Быстро обнаружить regression в новом коде, не перегружая систему постоянно.

#### 6. Экстраполяция метрик из sampled data

Если у тебя sampled data, как вычислить метрики для всего population?

**Counter (счетчик)**:
```
actual_count = sampled_count / sampling_rate
```

Пример: Увидел 10 errors при sampling rate 1%.
Реальное количество ≈ 10 / 0.01 = 1,000 errors.

**Percentiles (процентили)**:
Можно вычислять напрямую на sampled data (если sample достаточно большой и unbiased).

**Пример**: p99 latency на 10,000 sampled requests ≈ p99 latency на всех requests.

**Но**: Для extreme percentiles (p99.9, p99.99) нужны очень большие samples.

**Average (среднее)**:
```
average = sum(values) / count

На sampled data:
average = sum(sampled_values * weights) / sum(weights)
```

Если sampling uniform, то просто:
```
average = sum(sampled_values) / sampled_count
```

#### 7. Monitoring sampling effectiveness

Как понять, что твой sampling работает хорошо?

**Метрики sampling quality**:

**Coverage**: Покрываешь ли ты все strata?
```
coverage = (number of strata with samples) / (total strata)
```

Пример: Если у тебя 100 endpoints, и samples есть только для 70, то coverage = 70%.

**Bias**: Соответствует ли distribution sample distribution population?
```
bias = |sample_proportion - population_proportion|
```

Сравни error rate в sample vs error rate в full population (из counters).

**Effective sample size**: Сколько independent samples?
(При stratified или weighted sampling effective size может быть меньше actual size.)

**Completeness для traces**: Какой процент traces имеет все spans?
```
completeness = traces_with_all_spans / total_traces
```

#### 8. Fallback strategies для sampling failures

Что делать, когда sampling система не работает?

**Fail-safe defaults**:
- Если sampling decision service недоступен: fallback к fixed rate (например, 1%)
- Никогда не используй 0% (потеряешь всю visibility)
- Никогда не используй 100% (можешь перегрузить систему)

**Circuit breaker для sampling**:
```
if collector_is_overloaded:
  decrease_sampling_rate()

if collector_is_healthy:
  gradually_increase_sampling_rate()
```

**Local buffering**:
- Если collector недоступен, буферизуй samples локально
- Отправь, когда collector восстановится
- Но ограничь buffer size (не переполни disk)

---

## Взаимодействие семантик

Все эти семантики не существуют изолированно — они взаимодействуют и влияют друг на друга.

### Logging ↔ Tracing

**Correlation через trace_id**:
- Каждый log entry содержит trace_id
- Можно найти все логи для конкретного trace
- Можно найти trace для конкретного лога

**Пример workflow**:
1. Видишь ошибку в логах
2. Извлекаешь trace_id из лога
3. Открываешь trace в distributed tracing system
4. Видишь полный путь запроса
5. Для каждого span смотришь детальные логи

**Проблема**: Если trace был dropped из-за sampling, но лог остался — теряешь контекст.

**Решение**: Priority sampling — не drop traces с ошибками.

### Tracing ↔ Metrics

**Span-based metrics**:
- Из spans генерируешь метрики
- Например: latency histogram из span durations
- Error rate из spans с error=true

**Проблема**: Metrics на основе sampled traces неточны.

**Решение**:
- Генерируй metrics ДО sampling
- Или применяй sampling weights при агрегации

### Metrics ↔ Sampling

**Adaptive sampling на основе метрик**:
- Смотришь на текущую error rate (из metrics)
- Если error rate растет → увеличь sampling rate
- Если ошибок нет → уменьши sampling rate

**Feedback loop**:
```
Metrics → Sampling decision → Collected traces → Metrics → ...
```

### Event Ordering ↔ Tracing

**Causal ordering в traces**:
- Parent-child relationships определяют happened-before
- Дополняет timestamp-based ordering

**Проблема**: Clock skew может нарушить visual представление trace, даже если causal order правилен.

**Решение**: Trace visualizers должны опираться на parent-child, а не только на timestamps.

### Sampling ↔ Event Ordering

**Incomplete traces нарушают ordering**:
- Если spans dropped, теряешь часть causal chain
- Невозможно восстановить полный порядок

**Пример**:
```
Span A → Span B → Span C → Span D

Если Span C dropped:
Span A → Span B → ??? → Span D

Связь между B и D непонятна
```

**Решение**: Consistent sampling — если sample trace, то sample ВСЕ его spans.

### Комплексный пример: Debugging инцидента

**Сценарий**: Пользователь жалуется на медленный checkout.

**Шаг 1: Metrics** (Что происходит?)
- Смотришь на dashboard
- Видишь: p99 latency для `/api/checkout` вырос с 500ms до 3000ms
- Видишь: error rate вырос с 0.1% до 2%

**Шаг 2: Tracing** (Где bottleneck?)
- Фильтруешь traces: endpoint="/api/checkout", latency > 2s
- Выбираешь репрезентативный trace
- Видишь: 90% времени уходит на span "Payment Service: process_payment"

**Шаг 3: Logging** (Почему медленно?)
- Извлекаешь trace_id из медленного trace
- Ищешь логи с этим trace_id
- Видишь в логах Payment Service: "Database connection pool exhausted, waiting for connection"

**Шаг 4: Causal analysis** (Что привело к этому?)
- Смотришь на timeline в trace
- Видишь: непосредственно перед проблемой был deploy новой версии Inventory Service
- Inventory Service начал делать больше запросов к Payment Service
- Payment Service не справляется с increased load

**Шаг 5: Root cause**
- Новый код в Inventory Service вызывает Payment Service 3 раза вместо 1
- Connection pool в Payment Service не рассчитан на такую нагрузку

**Решение**:
- Откат deploy Inventory Service (краткосрочно)
- Увеличить connection pool в Payment Service (среднесрочно)
- Оптимизировать код в Inventory Service (долгосрочно)

Этот workflow был возможен благодаря комбинации всех observability семантик:
- Metrics показали проблему
- Tracing локализовал bottleneck
- Logging дал детали
- Event ordering установил причинность
- Sampling позволил найти репрезентативные traces

---

## Заключение

**Observability Semantics** — это фундаментальные правила, определяющие, насколько ты можешь доверять данным о своей системе. Понимание этих правил критически важно для:

1. **Правильной интерпретации данных**
   - Не все, что ты видишь в дашбордах, — абсолютная истина
   - Всегда есть ограничения и погрешности
   - Важно понимать, где данные могут быть неполными или неточными

2. **Эффективного troubleshooting**
   - Зная семантику, ты можешь быстрее найти причину проблемы
   - Понимаешь, какие данные искать и где
   - Избегаешь ложных выводов из-за artifacts observability системы

3. **Проектирования надежных систем**
   - Выбираешь правильные trade-offs между полнотой и overhead
   - Проектируешь observability с учетом реальных ограничений
   - Не полагаешься на то, что не гарантируется

### Ключевые принципы

**🔍 Нет идеальной observability**
- Всегда есть компромиссы между точностью, полнотой и overhead
- Невозможно собирать все данные обо всем
- Выбирай стратегию, подходящую для твоего случая

**⚖️ Trade-offs везде**
- Больше данных = больше overhead
- Точность vs performance
- Полнота vs cost
- Real-time vs completeness

**🔗 Корреляция критична**
- Связывай logs, metrics и traces
- Используй trace_id, request_id, session_id
- Не полагайся только на timestamps

**📊 Sampling — необходимость, а не зло**
- При правильном подходе можешь получить достаточную visibility с приемлемым overhead
- Но всегда помни о limitations sampling
- Адаптируй стратегию под свои нужды

**🕰️ Время в распределенных системах сложно**
- Clock skew реален
- Не полагайся только на timestamps для ordering
- Используй causal relationships где возможно

### Практические рекомендации

1. **Начни с базового observability**
   - RED метрики для сервисов
   - USE метрики для ресурсов
   - Структурированное логирование
   - Базовый distributed tracing

2. **Постепенно улучшай**
   - Добавляй correlation IDs
   - Внедряй priority sampling
   - Настраивай правильные retention policies
   - Мониторь качество observability данных

3. **Документируй свою семантику**
   - Какие гарантии даются?
   - Какие limitations есть?
   - Как интерпретировать данные?
   - Какие known issues?

4. **Регулярно проверяй**
   - Работает ли sampling как ожидается?
   - Теряются ли критичные события?
   - Достаточно ли sample size для твоих метрик?
   - Синхронизированы ли часы на серверах?

5. **Учись на инцидентах**
   - Каждый инцидент — возможность улучшить observability
   - Были ли нужные данные для расследования?
   - Что помешало быстро найти причину?
   - Как улучшить observability для следующего раза?

### Будущее observability

Observability продолжает развиваться:

**OpenTelemetry** — стандартизация:
- Единый способ сбора logs, metrics, traces
- Vendor-neutral
- Automatic instrumentation

**AI/ML для observability**:
- Автоматическое обнаружение аномалий
- Предсказание проблем
- Intelligent sampling

**Continuous profiling**:
- Постоянный сбор performance profiles
- Связь с traces для глубокого анализа

**eBPF для observability**:
- Kernel-level observability без изменения кода
- Минимальный overhead
- Visibility в system calls, network, etc.

Но фундаментальные семантики остаются: ты всегда будешь иметь дело с sampling, ordering, completeness и accuracy. Понимание этих концепций останется критически важным.

---

**Помни**: Observability — это не только инструменты и метрики. Это способность понимать свою систему. А понимание начинается со знания того, что данные могут и чего не могут тебе сказать. Это и есть Observability Semantics.
