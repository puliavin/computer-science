# Delivery Semantics (Семантики Доставки)

## Оглавление

1. [Введение](#введение)
2. [Классификация семантик доставки](#классификация-семантик-доставки)
3. [No Delivery Guarantees (Отсутствие Гарантий Доставки)](#1-no-delivery-guarantees-отсутствие-гарантий-доставки)
4. [Best Effort (Наилучшие Усилия)](#2-best-effort-наилучшие-усилия)
5. [At Most Once (Не Более Одного Раза)](#3-at-most-once-не-более-одного-раза)
6. [At Least Once (Как Минимум Один Раз)](#4-at-least-once-как-минимум-один-раз)
7. [Exactly Once (Ровно Один Раз)](#5-exactly-once-ровно-один-раз--логическая-гарантия)
8. [Effectively Once (Эффективно Один Раз)](#6-effectively-once-эффективно-один-раз)
9. [Сравнительная таблица](#сравнительная-таблица)
10. [Выбор правильной семантики: Decision Tree](#выбор-правильной-семантики-decision-tree)
11. [Практические рекомендации](#практические-рекомендации)
12. [Типичные ошибки и как их избежать](#типичные-ошибки-и-как-их-избежать)
13. [Заключение](#заключение)

---

## Введение

**Delivery Semantics** (семантики доставки) — это набор гарантий, которые определяют, как сообщения или данные доставляются между компонентами распределённой системы. Это один из фундаментальных аспектов проектирования надёжных систем.

Представь, что ты отправляешь письмо другу. Обычная почта не гарантирует доставку — письмо может потеряться. Заказное письмо даёт гарантию: ты получаешь уведомление о доставке. Но что, если почтальон доставил письмо дважды по ошибке? Или письмо пришло, но друг не смог его прочитать? Все эти ситуации иллюстрируют разные типы гарантий доставки.

В распределённых системах (Kafka, очереди сообщений, базы данных, микросервисы) понимание семантик доставки критически важно. Неправильный выбор или непонимание семантики может привести к:
- 💸 Двойным списаниям денег со счёта
- 📦 Потере важных событий (например, заказ оформлен, но уведомление не отправлено)
- 🔄 Бесконечным повторам одной и той же операции
- 🐛 Сложно диагностируемым багам в production

---

## Классификация семантик доставки

Существует несколько уровней гарантий, от самых слабых до самых строгих:

```
Нет гарантий → Best Effort → At Most Once → At Least Once → Exactly Once / Effectively Once
     ↓              ↓              ↓               ↓                    ↓
  Самое быстрое                                              Самое надёжное
  Самое простое                                              Самое сложное
```

---

## 1. No Delivery Guarantees (Отсутствие Гарантий Доставки)

### 📋 Определение

**No Delivery Guarantees** — это ситуация, когда система вообще не предоставляет никаких гарантий относительно доставки сообщения. Отправитель посылает данные и даже не пытается узнать, дошли они или нет.

### ✅ Что гарантирует

Абсолютно ничего. Это самый слабый уровень гарантий.

### ❌ Что НЕ гарантирует

- Сообщение может вообще не дойти
- Отправитель не узнает о неудаче
- Нет механизмов подтверждения или повтора
- Нет даже попытки обеспечить надёжность

### 🌍 Реальные примеры

**UDP протокол**
Когда ты смотришь видео-стрим или играешь в онлайн-игру, часто используется UDP. Если несколько пакетов потеряются — ничего страшного, видео чуть-чуть "подтормозит", но общий поток продолжится. Никто не пытается переслать потерянные пакеты.

**Метрики и логи (некритичные)**
Представьте систему мониторинга, которая собирает миллионы метрик в секунду. Если 0.1% метрик потеряются — это обычно приемлемо. Важнее не создавать overhead на подтверждениях.

**Fire-and-forget запросы**
Аналитика просмотров на сайте: отправили событие "пользователь открыл страницу" — и забыли. Если событие потерялось, статистика будет чуть менее точной, но сайт продолжит работать.

### ⚠️ Типичные проблемы

**Потеря данных без осознания**
Ты думаешь, что данные отправляются, но на самом деле 10% событий теряются из-за сбоев сети. Ты об этом не знаешь, потому что нет механизмов отслеживания.

**Невозможность отладки**
Когда пользователь жалуется "я нажал кнопку, но ничего не произошло", ты не можешь определить, дошёл ли запрос до сервера, потому что никакого подтверждения не было.

### 🏗️ Как проектировать систему

**Используйте только для некритичных данных:**
- Метрики мониторинга (где потеря нескольких точек данных приемлема)
- Preliminary аналитика
- Real-time стримы (где актуальность важнее точности)

**Добавляйте мониторинг на стороне получателя:**
Даже если отправитель не отслеживает доставку, получатель может мониторить "сколько сообщений я получаю в секунду" и алертить, если поток внезапно упал.

**Документируйте риски:**
Явно указывайте, что эта часть системы может терять данные, чтобы все понимали ограничения.

---

## 2. Best Effort (Наилучшие Усилия)

### 📋 Определение

**Best Effort** — система пытается доставить сообщение, но не гарантирует успех. Это значит: "Мы сделаем всё, что можем, но если что-то пойдёт не так — извините, повторять не будем".

### ✅ Что гарантирует

- Система **попытается** доставить сообщение
- Базовая проверка на уровне сети (checksums, routing)
- Если канал доступен и нет явных ошибок — сообщение дойдёт

### ❌ Что НЕ гарантирует

- Доставку при сбоях (сеть упала, получатель недоступен)
- Повторы при ошибках
- Уведомление отправителя о неудаче
- Гарантированную доставку в сложных условиях

### 🌍 Реальные примеры

**TCP без application-level acknowledgments**
TCP гарантирует доставку пакетов между двумя точками сети, но если сервер получил пакет и упал до обработки — данные потеряны. TCP сделал своё дело (best effort на сетевом уровне), но application-level доставка не гарантирована.

**DNS запросы**
Когда ты вбиваешь адрес сайта, DNS-сервер попытается найти IP-адрес. Если сервер недоступен или перегружен, запрос может не получить ответа. Система попыталась, но не гарантирует результат.

**HTTP запросы без retry логики**
Ты отправил HTTP запрос на сервер. Если сеть оборвалась в процессе — запрос не дойдёт. Клиент может получить ошибку, но автоматического повтора не будет.

**Email без подтверждения прочтения**
Ты отправил email. Почтовый сервер попытается его доставить, но если почтовый ящик получателя переполнен или его сервер недоступен — письмо может не дойти. Ты не всегда узнаешь об этом.

### ⚠️ Типичные проблемы

**Молчаливая потеря данных при сбоях**
Сервис отправил событие в Kafka, но Kafka-брокер в этот момент перезагружался. Событие потеряно. Сервис думает, что всё ок, потому что не проверял acknowledgment.

**Проблема "я не знаю, дошло ли"**
Представь платёжную систему с best effort: ты инициировал перевод денег, но не знаешь, прошёл он или нет. Отправить повторно? Риск дублирования. Не отправлять? Риск потери транзакции.

**Инцидент: потеря критичных событий**
В реальной системе событие "пользователь зарегистрировался" отправлялось в аналитику через best effort механизм. Во время сетевого сбоя 30% регистраций не попали в аналитику, и маркетинговая команда принимала решения на неполных данных.

### 🏗️ Как проектировать систему

**Подходит для толерантных к потерям сценариев:**
- Кеширование (можно пересчитать)
- Некритичная аналитика
- Уведомления, где потеря одного из тысячи приемлема

**Добавляйте мониторинг успешности:**
Даже если система best effort, мониторьте процент неудачных попыток. Если он растёт — это сигнал проблем.

**Документируйте поведение при сбоях:**
Команда должна понимать: "если Redis упадёт, кеш не обновится, но система продолжит работать".

**Комбинируйте с идемпотентностью:**
Если ты всё же добавишь retry логику поверх best effort, сделай операции идемпотентными.

---

## 3. At Most Once (Не Более Одного Раза)

### 📋 Определение

**At Most Once** — гарантия, что сообщение будет доставлено либо один раз, либо ни разу. Дубликаты **полностью исключены**, но доставка **не гарантирована**.

Это как отправить ценное письмо один раз и никогда не отправлять повторно, даже если не пришло подтверждение о доставке.

### ✅ Что гарантирует

- ✅ Нет дубликатов (сообщение обработается максимум один раз)
- ✅ Простота обработки на стороне получателя (не нужна дедупликация)

### ❌ Что НЕ гарантирует

- ❌ Доставку сообщения (может быть потеряно)
- ❌ Повторную отправку при сбое

### 🌍 Реальные примеры

**Kafka с auto-commit до обработки**
Consumer читает сообщение из Kafka и сразу коммитит offset (до обработки). Затем начинает обработку. Если consumer упал в процессе обработки — сообщение потеряно. Kafka считает, что оно обработано (offset сдвинут), повторно не отправит.

```
1. Consumer читает message #100
2. Consumer коммитит offset #100 (говорит Kafka: "я обработал до 100")
3. Consumer начинает обработку
4. 💥 Consumer падает
5. Новый consumer начинает с #101 → message #100 потерян навсегда
```

**UDP с sequence numbers**
Если отправитель нумерует UDP-пакеты и никогда не отправляет один и тот же номер дважды, получатель знает: если пакет потерян — он потерян навсегда, дубликата не будет.

**Circuit breaker с отключением retry**
Ты отправляешь запрос на внешний сервис. Если он недоступен, circuit breaker "открывается" и больше не пытается отправить запрос. Запрос либо прошёл с первого раза, либо не прошёл вообще — дубликатов нет.

### ⚠️ Типичные проблемы

**Потеря критичных данных**
**Инцидент:** Платёжная система использовала At Most Once для отправки уведомлений о транзакциях. Во время сбоя сервиса уведомлений 500 клиентов не получили SMS о списании денег. Клиенты звонили в поддержку, не понимая, куда делись деньги. Деньги списались, но уведомление потерялось.

**Несогласованность данных**
Ты обновляешь данные в базе и отправляешь событие в Kafka для синхронизации с другим сервисом. Событие потерялось (At Most Once). Теперь в разных частях системы разные версии данных.

**Невозможность восстановления**
Система логирования использует At Most Once. Во время кратковременного сетевого сбоя потеряно 1000 логов. Ты никогда не узнаешь, что произошло в системе в тот момент — данные утрачены навсегда.

### 🏗️ Как проектировать систему

**Подходит для:**
- Операции, где дубликаты катастрофичны, а потеря приемлема
- Системы, где идемпотентность невозможна или очень дорога
- Метрики, где важнее не дублировать данные, чем собрать всё

**Не подходит для:**
- ❌ Финансовые транзакции
- ❌ Критичные бизнес-события
- ❌ Данные, которые невозможно восстановить

**Добавляйте альтернативные источники данных:**
Если используете At Most Once для логов, дублируйте критичные события в более надёжное хранилище.

**Мониторьте потери:**
Отслеживайте "сколько сообщений мы должны были получить" vs "сколько получили". Это поможет оценить масштаб потерь.

**Используйте для идемпотентных операций:**
Чтение данных, запросы, которые не меняют состояние — если операция идемпотентна, потеря менее критична.

---

## 4. At Least Once (Как Минимум Один Раз)

### 📋 Определение

**At Least Once** — гарантия, что сообщение **обязательно** будет доставлено, но может быть доставлено **несколько раз**. Система повторяет попытки до успеха, что может привести к дубликатам.

Это как настойчивый продавец: он позвонит тебе раз, два, три... пока ты не ответишь. Но если соединение оборвалось, он может не понять, что ты уже ответил, и позвонит снова.

### ✅ Что гарантирует

- ✅ Сообщение **точно** будет доставлено (не потеряется)
- ✅ Повторные попытки при сбоях
- ✅ Надёжность доставки

### ❌ Что НЕ гарантирует

- ❌ Отсутствие дубликатов (одно сообщение может обработаться 2, 3, 10 раз)
- ❌ Порядок обработки (при параллельных повторах)

### 🌍 Реальные примеры

**Kafka с commit после обработки**
Consumer читает сообщение, обрабатывает его, и только потом коммитит offset. Если consumer упадёт после обработки, но до коммита, следующий consumer прочитает то же сообщение снова.

```
1. Consumer читает message #100
2. Consumer обрабатывает (например, пишет в БД)
3. 💥 Consumer падает ДО коммита offset
4. Новый consumer начинает с #100 (последний закоммиченный offset)
5. Message #100 обрабатывается второй раз → дубликат!
```

**Очереди сообщений (RabbitMQ, SQS) с visibility timeout**
Сообщение берётся из очереди, обрабатывается. Если worker не успел подтвердить обработку до истечения timeout, сообщение снова становится видимым и другой worker его заберёт.

**Инцидент:** Worker обрабатывал платёж 30 секунд (долгий API-вызов), но visibility timeout был 20 секунд. Второй worker взял то же сообщение и обработал параллельно. Результат: двойное списание денег.

**HTTP retry с таймаутами**
Ты отправляешь HTTP запрос на создание заказа. Запрос долго выполняется, клиент получает timeout и повторяет. Но первый запрос на самом деле успешно создал заказ — теперь два заказа вместо одного.

**Email с повторной отправкой**
Система отправки email пытается отправить письмо. SMTP-сервер принял письмо, но не успел ответить "OK" (сеть упала). Система решает, что письмо не отправлено, и отправляет снова. Пользователь получает два одинаковых письма.

### ⚠️ Типичные проблемы

**Дублирование критичных операций**

**Инцидент #1: Двойные платежи**
E-commerce система использовала At Least Once для обработки заказов. Worker обработал заказ (списал деньги), но не успел подтвердить обработку до крэша. Новый worker обработал заказ снова. С клиента списали деньги дважды. Массовые обращения в поддержку, ручные возвраты, потеря доверия.

**Инцидент #2: Дублирование уведомлений**
Система рассылки уведомлений работала с At Least Once. Во время сетевого сбоя некоторые сообщения отправились по 5-10 раз. Пользователи получили десятки одинаковых push-уведомлений. Резкий рост unsubscribe.

**Инцидент #3: Дубликаты в аналитике**
Событие "покупка совершена" обработалось дважды. В отчётах выручка показывалась завышенной на 15%. Бизнес принимал решения на основе некорректных данных.

**Проблема неидемпотентных операций**
Представьте операцию "увеличить счётчик на 1". При дубликате счётчик увеличится на 2. Или "отправить письмо" — отправится дважды.

**Проблема порядка**
Два сообщения: "установить цену 100" и "установить цену 200". При повторах порядок может нарушиться: сначала обработается "200", потом retry "100", итоговая цена будет неправильной.

### 🏗️ Как проектировать систему

**Обязательная идемпотентность!**

Все операции, обрабатывающие At Least Once сообщения, ДОЛЖНЫ быть идемпотентными.

**Примеры идемпотентных операций:**
- ✅ UPDATE user SET status='active' WHERE id=123 (выполнить дважды = тот же результат)
- ✅ PUT запрос с полной заменой данных
- ✅ Проверка "если запись существует — обновить, иначе создать"

**Примеры НЕидемпотентных операций:**
- ❌ UPDATE user SET balance = balance - 100 (дважды = списание 200)
- ❌ POST запрос на создание ресурса (дважды = два ресурса)
- ❌ Отправка email без проверки

**Дедупликация на уровне приложения:**

**Используйте уникальные идентификаторы (idempotency keys):**
Каждое сообщение имеет уникальный ID. При обработке проверяйте: "обрабатывал ли я это ID раньше?". Если да — пропускаете.

```
Сообщение: {id: "msg-12345", action: "create_order", ...}

Обработка:
1. Проверить в БД: есть ли запись о processed_message_id = "msg-12345"?
2. Если есть → пропустить (уже обработано)
3. Если нет → обработать + записать "msg-12345" в БД как обработанное
```

**Используйте транзакции:**
Обработка сообщения и запись "обработано" должны быть в одной транзакции.

**Redis для быстрой дедупликации:**
Храните обработанные IDs в Redis (с TTL) для быстрой проверки.

**Мониторьте дубликаты:**
Отслеживайте, как часто происходят повторы. Если слишком часто — это сигнал проблем (например, слишком короткий timeout).

**Используйте версионирование:**
При обновлениях добавляйте version field. Обновление применяется только если version в БД совпадает с ожидаемым.

---

## 5. Exactly Once (Ровно Один Раз) — Логическая Гарантия

### 📋 Определение

**Exactly Once** — гарантия, что сообщение будет обработано **ровно один раз**: не потеряется и не задублируется.

⚠️ **Важное уточнение:** "Настоящий" Exactly Once в распределённых системах **теоретически недостижим** из-за фундаментальных ограничений (CAP-теорема, невозможность отличить "медленный ответ" от "сбой"). Однако на практике достигается **логический Exactly Once** через комбинацию техник.

### ✅ Что гарантирует

- ✅ Сообщение не потеряется
- ✅ Сообщение не обработается дважды
- ✅ Предсказуемое поведение системы

### ❌ Что НЕ гарантирует (нюансы)

Даже системы, заявляющие Exactly Once, имеют ограничения:
- Обычно гарантия работает **в пределах одной системы** (например, внутри Kafka)
- **Не гарантирует** Exactly Once при взаимодействии с внешними системами без дополнительных мер
- Зависит от корректной конфигурации и использования

### 🌍 Реальные примеры

**Kafka Exactly Once Semantics (EOS)**

Kafka поддерживает Exactly Once между producer и consumer **внутри экосистемы Kafka** через:
- Transactional producers (транзакционные продюсеры)
- Idempotent producers (идемпотентные продюсеры)
- Transactional reads

Это работает так:
1. Producer отправляет сообщения в транзакции
2. Consumer читает только committed транзакции
3. Offset коммитится в той же транзакции, что и обработка

**Важно:** Это Exactly Once только в рамках Kafka. Если consumer пишет в базу данных, гарантия не распространяется на БД автоматически.

**Distributed transactions (2PC, Saga)**

Представьте перевод денег между двумя банками:
1. Списать 100$ со счёта А в банке X
2. Зачислить 100$ на счёт Б в банке Y

2PC (Two-Phase Commit) пытается сделать обе операции атомарными — либо обе выполнятся, либо обе откатятся. Но это медленно и хрупко.

**База данных с транзакциями**

Postgres с транзакциями обеспечивает Exactly Once **внутри одной базы**:
```
BEGIN;
  UPDATE accounts SET balance = balance - 100 WHERE id=1;
  UPDATE accounts SET balance = balance + 100 WHERE id=2;
COMMIT;
```
Либо обе операции выполнятся, либо ни одна. Дубликатов не будет, потери не будет.

**Stream processing frameworks (Flink, Kafka Streams)**

Apache Flink может гарантировать Exactly Once для state updates через механизм checkpoint:
1. Периодически сохраняет состояние всех операторов
2. При сбое откатывается к последнему checkpoint
3. Обработка идёт снова, но дубликаты фильтруются через state

### ⚠️ Типичные проблемы

**Ложное чувство безопасности**

**Инцидент:** Команда использовала Kafka EOS и думала, что все проблемы решены. Но consumer писал в PostgreSQL без транзакций. При сбое сообщение обработалось дважды (один раз до сбоя частично записано в БД, второй раз после restart). Exactly Once в Kafka не защитил от дубликатов в БД.

**Производительность и сложность**

Exactly Once требует:
- Транзакций (медленно)
- Координации между компонентами (сложно)
- Дополнительных round-trips (latency)

**Инцидент:** Компания включила Kafka EOS для всех топиков. Throughput упал на 40%, latency вырос в 3 раза. Для большинства топиков At Least Once + идемпотентность было бы достаточно и быстрее.

**Не работает при взаимодействии с внешними системами**

Представь: ты отправляешь email через внешний API. Даже с Exactly Once в Kafka, если ты отправил email, а потом упал до коммита offset, при повторе email отправится снова. Внешний API не знает про твою транзакцию.

**Недопонимание гарантий**

Exactly Once часто означает "обработано ровно один раз с точки зрения логики", но физически операции могут повторяться (с идемпотентностью, дедупликацией).

### 🏗️ Как проектировать систему

**Оцените необходимость:**
Действительно ли нужен Exactly Once? Часто At Least Once + идемпотентность проще, быстрее, и решает задачу.

**Используйте Exactly Once, если:**
- ✅ Критичные финансовые операции (где дубликаты недопустимы, а At Least Once + идемпотентность слишком рискованны)
- ✅ Строгое требование к consistency
- ✅ Высокая цена ошибки

**Не используйте Exactly Once, если:**
- ❌ Высокая нагрузка и требование к низкой latency
- ❌ Можно обеспечить идемпотентность проще
- ❌ Данные некритичны

**Комбинируйте техники:**

1. **Transactional outbox pattern**
   - Сохраняете событие в БД в той же транзакции, что и основную операцию
   - Отдельный процесс читает события из БД и отправляет в Kafka
   - Гарантирует: либо и операция, и событие выполнены, либо ничего

2. **Idempotency tokens**
   - Каждая операция имеет уникальный token
   - Повторное выполнение с тем же token игнорируется
   - Stripe API использует это для платежей

3. **Distributed transactions (осторожно!)**
   - 2PC работает, но медленно и хрупко
   - Saga pattern для long-running транзакций
   - Используйте только если действительно необходимо

**Тестируйте failure scenarios:**
Создавайте хаос-тесты: убивайте процессы в случайные моменты, имитируйте сетевые сбои. Проверяйте, что Exactly Once действительно работает.

**Мониторьте:**
Отслеживайте метрики транзакций, rollback, latency. Exactly Once дорого — убедитесь, что эта цена оправдана.

---

## 6. Effectively Once (Эффективно Один Раз)

### 📋 Определение

**Effectively Once** — это практический подход, где система технически использует **At Least Once**, но через идемпотентность и дедупликацию достигается **эффект Exactly Once**.

Это как отправка заказного письма с уникальным номером: письмо может прийти дважды (At Least Once), но получатель, видя номер, поймёт, что уже обработал это письмо, и проигнорирует дубликат.

Effectively Once — это **золотая середина**: надёжность Exactly Once при простоте и производительности At Least Once.

### ✅ Что гарантирует

- ✅ Сообщения не потеряются (благодаря At Least Once)
- ✅ Дубликаты не влияют на результат (благодаря идемпотентности)
- ✅ Проще и быстрее, чем "настоящий" Exactly Once
- ✅ Работает с внешними системами

### ❌ Что НЕ гарантирует

- ❌ Физическое отсутствие дубликатов (они могут быть, но нейтрализуются)
- ❌ Нулевую latency (дедупликация требует проверок)

### 🌍 Реальные примеры

**Платёжные системы с idempotency keys**

**Stripe API:**
При создании платежа ты передаёшь `Idempotency-Key`:
```
POST /v1/charges
Idempotency-Key: order-12345-payment-1

{amount: 1000, currency: "usd", ...}
```

Если ты отправишь этот запрос дважды (например, из-за retry), Stripe обработает платёж только один раз. Второй запрос вернёт результат первого.

**Как это работает:**
1. Stripe получает запрос с ключом "order-12345-payment-1"
2. Проверяет: обрабатывался ли этот ключ?
3. Если нет — обрабатывает, сохраняет результат с этим ключом
4. Если да — возвращает сохранённый результат, ничего не делая

**E-commerce с order ID**

При создании заказа генерируется уникальный order_id (например, UUID). При обработке:
```
1. Проверить: существует ли order с order_id в БД?
2. Если да → вернуть "заказ уже создан"
3. Если нет → создать заказ
```

Даже если сообщение о создании заказа придёт дважды (At Least Once), заказ создастся один раз.

**Kafka consumer с дедупликацией**

```
Сообщение: {message_id: "msg-abc-123", action: "add_to_cart", user_id: 42, item_id: 100}

Обработка:
1. Читаем из Kafka (At Least Once — может быть дубликат)
2. Проверяем в БД/Redis: обрабатывали message_id "msg-abc-123"?
3. Если да → skip
4. Если нет:
   - Выполняем действие (добавить товар в корзину)
   - Записываем в БД: processed_messages.insert({id: "msg-abc-123", timestamp: now()})
   - Коммитим offset
```

**Банковские переводы**

Перевод имеет уникальный transaction_id. База данных имеет constraint на transaction_id (уникальность). При попытке обработать дубликат:
```
INSERT INTO transactions (transaction_id, from_account, to_account, amount)
VALUES ('txn-123', 'A', 'B', 100);

Если txn-123 уже есть → constraint violation → rollback → игнорируем
```

### ⚠️ Типичные проблемы

**Проблема: забыли про идемпотентность**

**Инцидент:** Команда настроила Kafka с At Least Once, но не сделала обработчики идемпотентными. Каждый retry приводил к дубликатам. Например, "отправить уведомление" отправлялось по 3-5 раз, пока не обработается успешно.

**Проблема: неправильная дедупликация**

**Инцидент #1: Слишком короткий TTL**
Команда хранила processed message IDs в Redis с TTL 1 час. Но иногда retry приходил через 2 часа (после длительного сбоя). ID уже удалился из Redis → дубликат обработался.

**Решение:** TTL должен покрывать максимально возможное время retry + запас.

**Инцидент #2: Дедупликация не в транзакции**
```
1. Обработать сообщение (списать деньги)
2. Записать message_id в БД как processed
3. 💥 Сбой между 1 и 2
4. При retry: message_id нет в БД → обработать снова → двойное списание
```

**Решение:** Обработка и запись ID должны быть в одной транзакции.

**Проблема: race conditions при параллельной обработке**

Два воркера одновременно обрабатывают один и тот же дубликат:
```
Время 0: Worker A проверяет message_id → не найден
Время 1: Worker B проверяет message_id → не найден
Время 2: Worker A обрабатывает → записывает ID
Время 3: Worker B обрабатывает → записывает ID (дубликат!)
```

**Решение:** Используйте distributed locks или database constraints (unique index на message_id).

**Проблема: performance overhead**

Каждая обработка требует:
- Проверку в БД/Redis (exists message_id?)
- Запись результата
- Это добавляет latency

**Инцидент:** Система обрабатывала 100k сообщений/сек. После добавления дедупликации (проверка в Postgres) throughput упал до 20k/сек из-за overload на БД.

**Решение:** Используйте Redis для дедупликации (быстрее), батчинг, оптимизируйте индексы.

### 🏗️ Как проектировать систему

**1. Генерируйте уникальные идентификаторы**

**На стороне отправителя:**
- UUID для каждого сообщения/события
- Составные ключи (user_id + timestamp + action)
- Content-based hash (если сообщение с одинаковым содержимым должно обрабатываться один раз)

**2. Выбирайте storage для дедупликации**

**Redis:**
- ✅ Очень быстро
- ✅ TTL из коробки
- ❌ Не персистентный (может потерять данные при перезапуске)
- **Подходит:** для высоконагруженных систем, где потеря дедупликации не критична

**PostgreSQL/MySQL:**
- ✅ Персистентный
- ✅ Транзакции
- ✅ Constraints (unique index)
- ❌ Медленнее Redis
- **Подходит:** для критичных операций (платежи, заказы)

**Hybrid (Redis + DB):**
- Проверяем сначала Redis (быстро)
- Если не нашли — проверяем БД (надёжно)
- Записываем в оба

**3. Идемпотентные операции**

**Естественная идемпотентность:**
- SET key=value (сколько раз ни выполни — результат одинаковый)
- DELETE WHERE id=X
- UPDATE SET status='completed' WHERE id=X

**Искусственная идемпотентность через дедупликацию:**
- Проверка processed_ids
- Unique constraints
- Compare-and-swap operations

**4. Используйте транзакции**

Все критичные операции в транзакции:
```
BEGIN TRANSACTION;
  -- Проверить, обработано ли
  SELECT id FROM processed_messages WHERE message_id='msg-123' FOR UPDATE;

  -- Если не обработано
  IF NOT FOUND THEN
    -- Выполнить операцию
    UPDATE accounts SET balance = balance - 100 WHERE id=1;
    -- Записать как обработанное
    INSERT INTO processed_messages (message_id, processed_at) VALUES ('msg-123', NOW());
  END IF;
COMMIT;
```

**5. Мониторинг и алерты**

**Метрики:**
- Процент дубликатов (сколько сообщений пришли повторно)
- Latency дедупликации
- Размер processed_ids storage

**Алерты:**
- Резкий рост дубликатов → проблемы с retry логикой
- Переполнение storage для дедупликации

**6. Cleanup старых IDs**

Не храните IDs вечно:
- Redis: TTL (например, 7 дней)
- БД: периодическая очистка (DELETE FROM processed_messages WHERE processed_at < NOW() - INTERVAL '7 days')

**7. Тестируйте retry scenarios**

Создавайте тесты, где:
- Сообщение обрабатывается дважды
- Worker падает в разные моменты
- Параллельная обработка дубликатов

---

## Сравнительная таблица

| Семантика | Потеря возможна? | Дубликаты возможны? | Сложность | Производительность | Когда использовать |
|-----------|-----------------|---------------------|-----------|-------------------|-------------------|
| **No Delivery Guarantees** | ✅ Да | ✅ Да | 🟢 Простая | 🟢 Высокая | Некритичные данные, где важнее скорость |
| **Best Effort** | ✅ Да | ✅ Да | 🟢 Простая | 🟢 Высокая | Некритичная аналитика, кеши |
| **At Most Once** | ✅ Да | ❌ Нет | 🟡 Средняя | 🟢 Высокая | Когда дубликаты хуже потерь, идемпотентность невозможна |
| **At Least Once** | ❌ Нет | ✅ Да | 🟡 Средняя | 🟡 Средняя | Большинство случаев (с идемпотентностью) |
| **Exactly Once** | ❌ Нет | ❌ Нет | 🔴 Высокая | 🔴 Низкая | Критичные операции, где необходима строгая гарантия |
| **Effectively Once** | ❌ Нет | Физически да, логически нет | 🟡 Средняя | 🟡 Средняя | **Оптимальный выбор** для большинства продакшен систем |

---

## Выбор правильной семантики: Decision Tree

```
Может ли сообщение потеряться?
    ├─ ДА → Насколько критична потеря?
    │       ├─ Некритична → Best Effort / At Most Once
    │       └─ Критична → ❌ Неподходящие варианты
    │
    └─ НЕТ → Допустимы ли дубликаты?
            ├─ ДА → Можете сделать операцию идемпотентной?
            │       ├─ ДА → ✅ Effectively Once (рекомендуется)
            │       └─ НЕТ → Exactly Once (дорого)
            │
            └─ НЕТ → Есть ли ресурсы на Exactly Once?
                    ├─ ДА → Exactly Once
                    └─ НЕТ → Effectively Once (лучшая альтернатива)
```

---

## Практические рекомендации

### 🎯 Правило #1: По умолчанию используйте Effectively Once

Для 90% задач **Effectively Once** (At Least Once + идемпотентность) — оптимальный выбор:
- ✅ Надёжность (нет потерь)
- ✅ Производительность (быстрее Exactly Once)
- ✅ Простота (проще Exactly Once)
- ✅ Работает с любыми системами

### 🎯 Правило #2: Думайте об идемпотентности на этапе проектирования

Не ждите проблем с дубликатами — проектируйте идемпотентные операции с самого начала:
- Используйте уникальные ID для всех важных операций
- Проектируйте API с idempotency keys
- Используйте PUT вместо POST, где возможно

### 🎯 Правило #3: Мониторьте реальное поведение

Теоретические гарантии != реальность. Мониторьте:
- Сколько сообщений теряется (даже при At Least Once — неправильная конфигурация может приводить к потерям)
- Сколько дубликатов обрабатывается
- Latency обработки

### 🎯 Правило #4: Документируйте выбор семантики

Для каждой интеграции явно документируйте:
- Какую семантику используете
- Почему выбрали именно её
- Какие механизмы обеспечивают гарантии
- Какие edge cases могут возникнуть

### 🎯 Правило #5: Тестируйте failure scenarios

Chaos engineering:
- Убивайте процессы в случайные моменты
- Имитируйте сетевые задержки и разрывы
- Тестируйте поведение при параллельной обработке дубликатов

### 🎯 Правило #6: Не переплачивайте за гарантии

Exactly Once дорого. Оцените реальную необходимость:
- Действительно ли дубликаты катастрофичны?
- Можете ли сделать операцию идемпотентной?
- Стоит ли overhead на производительности?

---

## Типичные ошибки и как их избежать

### ❌ Ошибка #1: "У нас Kafka, значит Exactly Once"

Kafka EOS работает только внутри Kafka. Если ты пишешь в PostgreSQL или вызываешь внешний API — гарантии не распространяются.

**Решение:** Используйте Effectively Once с идемпотентностью.

### ❌ Ошибка #2: "Мы делаем retry, значит At Least Once"

Retry без подтверждения доставки — это всё ещё Best Effort.

**Решение:** Убедитесь, что есть acknowledgment и retry продолжается до успеха.

### ❌ Ошибка #3: "Операция быстрая, дубликаты невозможны"

Race conditions могут случиться даже за миллисекунды.

**Решение:** Всегда защищайтесь от дубликатов (idempotency, constraints).

### ❌ Ошибка #4: "Дедупликация решает всё"

Дедупликация с race conditions или без транзакций может пропустить дубликаты.

**Решение:** Используйте locks или database constraints.

### ❌ Ошибка #5: "Exactly Once слишком сложно, используем At Most Once"

Потери данных могут быть хуже дубликатов.

**Решение:** Используйте Effectively Once как компромисс.

---

## Заключение

**Delivery Semantics** — это не просто теоретическая концепция, а фундаментальный выбор, влияющий на надёжность, производительность и сложность системы.

### Ключевые выводы:

1. **Нет универсального решения** — выбор семантики зависит от конкретной задачи

2. **Effectively Once — золотой стандарт** для большинства продакшен систем

3. **Идемпотентность — must-have** при работе с At Least Once

4. **Exactly Once** дорого и сложно — используйте только когда действительно необходимо

5. **Мониторинг и тестирование** критичны — теория должна соответствовать практике

6. **Документируйте выбор** — команда должна понимать, какие гарантии предоставляет система

---

### Дополнительные ресурсы для изучения

**Почитать:**
- Kafka Documentation: Delivery Semantics
- Google Cloud Pub/Sub: Message delivery semantics
- AWS SQS: Standard vs FIFO queues
- Designing Data-Intensive Applications (Martin Kleppmann) — глава про репликацию и консистентность

**Паттерны:**
- Transactional Outbox Pattern
- Saga Pattern
- Idempotent Consumer Pattern
- Retry with Exponential Backoff

**Системы для изучения:**
- Apache Kafka (все три семантики)
- RabbitMQ (At Most Once, At Least Once)
- Google Cloud Pub/Sub (At Least Once)
- Apache Flink (Exactly Once для state)

---

**Помните:** Правильный выбор Delivery Semantics — это баланс между надёжностью, производительностью и сложностью. Начните с понимания требований вашей системы, оцените риски, и выбирайте семантику осознанно! 🎯
