# Security & Trust Semantics (Семантики Безопасности и Доверия)

## Оглавление

1. [Введение](#введение)
2. [Authentication Semantics (Семантика Аутентификации)](#1-authentication-semantics-семантика-аутентификации)
3. [Authorization Semantics (Семантика Авторизации)](#2-authorization-semantics-семантика-авторизации)
4. [Trust Model Semantics (Семантика Моделей Доверия)](#3-trust-model-semantics-семантика-моделей-доверия)
5. [Zero-Trust Semantics (Семантика Нулевого Доверия)](#4-zero-trust-semantics-семантика-нулевого-доверия)
6. [Byzantine Trust Assumptions (Византийские Предположения о Доверии)](#5-byzantine-trust-assumptions-византийские-предположения-о-доверии)
7. [Сравнительная таблица](#сравнительная-таблица)
8. [Комбинации семантик](#комбинации-семантик)
9. [Практические рекомендации](#практические-рекомендации)
10. [Типичные ошибки и как их избежать](#типичные-ошибки-и-как-их-избежать)
11. [Заключение](#заключение)

---

## Введение

**Security & Trust Semantics** (семантики безопасности и доверия) определяют, КАК система проверяет идентичность участников, контролирует доступ к ресурсам и принимает решения о том, кому и чему можно доверять. Это фундаментальные характеристики, которые влияют на безопасность, устойчивость к атакам и способность системы работать в условиях недоверия.

Представь многоквартирный дом с разными уровнями безопасности:

- **Authentication (аутентификация)**: домофон проверяет, что ты действительно житель дома (по коду или ключу)
- **Authorization (авторизация)**: ключ от твоей квартиры не откроет чужую дверь, даже если ты житель дома
- **Trust Model (модель доверия)**: ты доверяешь соседям по площадке больше, чем незнакомцам с улицы
- **Zero-Trust (нулевое доверие)**: каждый раз проверять документы, даже если человек вошёл в подъезд
- **Byzantine Trust (византийское доверие)**: предполагать, что некоторые соседи могут злонамеренно врать о том, что происходит в доме

В распределённых системах выбор правильной семантики безопасности критичен. Неправильный выбор может привести к:

- 🔓 **Несанкционированному доступу**: злоумышленник получает доступ к данным клиентов
- 🎭 **Подмене идентичности**: кто-то выдаёт себя за легитимный сервис
- 🔥 **Повышению привилегий**: обычный пользователь получает права администратора
- 💣 **Byzantine failures**: злонамеренные узлы саботируют работу системы
- 🕵️ **Lateral movement**: взломав один сервис, злоумышленник получает доступ ко всем остальным
- 📊 **Утечкам данных**: отсутствие проверки разрешений при репликации данных

---

## 1. Authentication Semantics (Семантика Аутентификации)

### 📋 Определение

**Authentication Semantics** — это набор правил и гарантий, определяющих, КАК система проверяет идентичность участников (пользователей, сервисов, узлов). Аутентификация отвечает на вопрос: "Кто ты?", но не "Что тебе разрешено делать?".

```
Клиент                         Сервер
  |                               |
  |--- Credentials (токен) ------>|
  |                               |
  |                               | (проверяет подпись/хеш)
  |                               | (сверяет с базой)
  |                               |
  |<---- Authenticated/Denied ----|
  |                               |
```

### ✅ Что гарантирует

- **Identity Verification (проверка идентичности)**: подтверждение, что сущность — это именно та, за кого себя выдаёт
- **Non-repudiation (неотказуемость)**: невозможность отрицать совершённое действие (при использовании криптографии)
- **Proof of Possession (доказательство владения)**: подтверждение владения секретом (паролем, ключом, сертификатом)
- **Session Establishment (установление сессии)**: создание контекста для последующих запросов

### ❌ Что НЕ гарантирует

- **Не определяет разрешения**: аутентификация не означает авторизацию
- **Не защищает от утечки credentials**: если токен украден, система не узнает о подмене
- **Не гарантирует актуальность**: токены могут быть валидными, но пользователь уже удалён
- **Не защищает от replay attacks**: без дополнительных механизмов (nonce, timestamp) старые токены можно переиспользовать

### 🌍 Реальные примеры

#### **Kafka: SASL Authentication**

Kafka поддерживает несколько механизмов аутентификации:

**SASL/PLAIN** (базовый username/password):
- Клиент отправляет логин и пароль при подключении
- Broker проверяет credentials через JAAS (Java Authentication and Authorization Service)
- Если успешно, клиент получает authenticated connection
- Все последующие операции (produce/consume) идут через эту сессию

**SASL/SCRAM** (Salted Challenge Response Authentication Mechanism):
- Более безопасный механизм: пароль не передаётся в открытом виде
- Используется challenge-response: сервер отправляет случайное значение, клиент хеширует его с паролем
- Защищает от перехвата пароля, но требует нескольких раундов обмена сообщениями

**mTLS (Mutual TLS)**:
- Двусторонняя аутентификация: и клиент, и брокер проверяют сертификаты друг друга
- Каждый producer/consumer имеет свой X.509 сертификат
- Broker проверяет, что сертификат клиента подписан доверенным CA (Certificate Authority)
- Очень сильная гарантия идентичности, но сложная в управлении (ротация сертификатов)

Типичная проблема:
- Ты настроил mTLS, но забыл включить авторизацию
- Любой клиент с валидным сертификатом может читать ЛЮБОЙ топик
- Аутентификация есть, авторизации нет — это security gap

#### **PostgreSQL: Password vs Certificate Authentication**

**Password-based (md5/scram-sha-256)**:
```
# pg_hba.conf
host  mydb  myuser  10.0.0.0/8  scram-sha-256
```
- Клиент подключается с username и паролем
- Postgres хеширует пароль и сравнивает с `pg_authid`
- Если совпадает — аутентификация успешна

**Certificate-based**:
```
# pg_hba.conf
hostssl  mydb  myuser  10.0.0.0/8  cert
```
- Клиент предоставляет SSL-сертификат
- Postgres проверяет, что сертификат подписан доверенным CA
- Common Name (CN) в сертификате должен совпадать с username в Postgres

Важный нюанс:
- Если в `pg_hba.conf` указано `trust`, то аутентификация вообще не происходит
- Любой, кто может подключиться по сети, получает доступ
- Это катастрофа в продакшене, но иногда используется в dev-окружениях

#### **Redis: AUTH Command**

Redis изначально проектировался для работы в доверенной сети, поэтому аутентификация примитивна:

**До Redis 6.0**:
- Одна команда `AUTH password`
- Все клиенты используют один и тот же пароль
- Нет понятия "пользователей" — либо ты аутентифицирован, либо нет
- Пароль передаётся в открытом виде (требуется TLS)

**После Redis 6.0 (ACL)**:
```
ACL SETUSER alice on >password123 ~* +@all
AUTH alice password123
```
- Появились пользователи с разными паролями
- Можно назначать разные разрешения разным пользователям
- Но всё ещё нет поддержки сертификатов или внешних auth providers

Типичная ошибка:
- Разворачиваешь Redis без пароля в облаке с публичным IP
- Через 5 минут боты находят его и майнят криптовалюту
- Redis не имеет аутентификации по умолчанию — это must-do настройка

#### **MongoDB: Authentication Mechanisms**

MongoDB поддерживает несколько механизмов:

**SCRAM-SHA-256** (default):
```
use admin
db.createUser({
  user: "myuser",
  pwd: "mypassword",
  roles: ["readWrite"]
})
```
- Challenge-response аутентификация
- Пароль не передаётся в открытом виде

**x.509 Certificate Authentication**:
- Клиент использует сертификат вместо пароля
- MongoDB извлекает DN (Distinguished Name) из сертификата и мапит его на пользователя
- Сертификат должен быть подписан CA, указанным в конфигурации MongoDB

**LDAP/Kerberos** (Enterprise):
- Интеграция с корпоративным Active Directory
- Пользователи аутентифицируются через существующую инфраструктуру

Проблема масштаба:
- У тебя 100 микросервисов, каждому нужен доступ к MongoDB
- Если используешь пароли, нужно 100 секретов хранить и ротировать
- Если используешь сертификаты, нужна инфраструктура для их выпуска и обновления
- Решение: использовать service mesh с автоматической ротацией credentials

#### **Nginx: Basic Auth vs Client Certificates**

**HTTP Basic Auth**:
```
location /admin {
    auth_basic "Restricted Area";
    auth_basic_user_file /etc/nginx/.htpasswd;
}
```
- Браузер отправляет `Authorization: Basic base64(username:password)`
- Nginx декодирует и сравнивает с файлом `.htpasswd`
- Крайне слабая защита: пароль передаётся в каждом запросе

**Client Certificate Authentication**:
```
ssl_client_certificate /etc/nginx/ca.crt;
ssl_verify_client on;

location /api {
    if ($ssl_client_verify != SUCCESS) { return 403; }
}
```
- Клиент предоставляет сертификат при TLS handshake
- Nginx проверяет, что сертификат подписан доверенным CA
- Можно извлечь информацию из сертификата: `$ssl_client_s_dn`

Практический кейс:
- У тебя API для интеграции с партнёрами
- Каждому партнёру выдаёшь уникальный сертификат
- Nginx проверяет сертификат и передаёт идентификатор партнёра в бэкенд
- Бэкенд использует этот идентификатор для авторизации и биллинга

### 💥 Типичные проблемы и инциденты

**1. Отсутствие аутентификации на внутренних сервисах**

Инцидент:
- Компания полагалась на "security by obscurity": внутренние сервисы доступны только из приватной сети
- Злоумышленник получил доступ к одному из серверов через уязвимость
- Оттуда получил доступ ко ВСЕМ внутренним сервисам без аутентификации
- Lateral movement за считанные минуты

Урок: даже внутренние сервисы должны требовать аутентификацию.

**2. Использование слабых механизмов аутентификации**

Инцидент (реальный случай с Elasticsearch):
- Elasticsearch раньше не имел встроенной аутентификации (это было в X-Pack, платной версии)
- Многие разворачивали кластеры без защиты
- Публичные кластеры индексировались поисковиками
- Массовые утечки данных: медицинские записи, логи с токенами, PII

Урок: отсутствие аутентификации — это не просто риск, это гарантированная проблема.

**3. Неправильное управление session lifetime**

Проблема:
- JWT токен выдан с `exp` через 1 год
- Через месяц обнаруживается утечка токена
- Ревокация невозможна (JWT stateless)
- Токен будет валидным ещё 11 месяцев

Урок: короткие lifetime + refresh tokens + механизм ревокации.

**4. Replay attacks**

Инцидент:
- API принимает подписанный токен, но не проверяет timestamp или nonce
- Злоумышленник перехватывает валидный запрос
- Переотправляет его тысячи раз
- Система обрабатывает каждый как легитимный (например, списание денег)

Урок: включать timestamp, nonce или sequence number в подписанные сообщения.

**5. Credential stuffing из-за переиспользования паролей**

Проблема:
- Пользователь использует один пароль везде
- Утечка базы паролей на другом сайте
- Злоумышленник пробует те же credentials на твоём сервисе
- Успешная аутентификация

Защита: rate limiting, CAPTCHA, обнаружение аномалий (логин из нового города).

### 🛠 Практики проектирования

**1. Многофакторная аутентификация (MFA)**
- Комбинация: что-то, что ты знаешь (пароль) + что-то, что у тебя есть (токен, телефон)
- Даже если пароль украден, нужен второй фактор

**2. Short-lived credentials с автоматической ротацией**
- Токены с коротким TTL (минуты/часы, не дни)
- Refresh tokens для получения новых access tokens
- Автоматическая ротация API ключей (AWS IAM роли для EC2)

**3. Mutual TLS (mTLS) для service-to-service**
- Не только сервер проверяет клиента, но и клиент проверяет сервер
- Защита от MITM attacks
- Service mesh (Istio, Linkerd) автоматизирует это

**4. Centralized Authentication Service**
- OAuth 2.0 / OIDC (OpenID Connect)
- Единая точка аутентификации для всех сервисов
- Централизованная ротация ключей, отзыв токенов

**5. Rate limiting и брутфорс-защита**
- Ограничение попыток аутентификации с одного IP
- Exponential backoff после неудачных попыток
- CAPTCHA после N неудачных попыток

**6. Audit logging**
- Логировать все попытки аутентификации (успешные и неудачные)
- Алерты на аномалии (логин из новой страны, необычное время)
- Возможность расследования инцидентов

---

## 2. Authorization Semantics (Семантика Авторизации)

### 📋 Определение

**Authorization Semantics** — это набор правил и гарантий, определяющих, ЧТО аутентифицированный участник имеет право делать. Авторизация отвечает на вопрос: "Разрешено ли тебе выполнить это действие?".

```
Клиент (уже аутентифицирован)    Сервер
  |                                  |
  |--- Запрос: DELETE /users/123 -->|
  |                                  |
  |                                  | (проверяет ACL/RBAC)
  |                                  | (сопоставляет с ролью клиента)
  |                                  |
  |<---- 403 Forbidden или 200 OK ---|
  |                                  |
```

Критическое различие:
- **Authentication**: "Ты — это Алиса"
- **Authorization**: "Алиса может читать файл X, но не может его удалить"

### ✅ Что гарантирует

- **Access Control (контроль доступа)**: ограничение действий на основе политик
- **Principle of Least Privilege (принцип минимальных привилегий)**: выдача только необходимых разрешений
- **Separation of Duties (разделение обязанностей)**: критичные операции требуют согласия нескольких ролей
- **Audit Trail (аудит)**: возможность отследить, кто и что сделал

### ❌ Что НЕ гарантирует

- **Не заменяет аутентификацию**: нужно сначала узнать, кто делает запрос
- **Не защищает от логических багов**: если политика составлена неправильно, авторизация бесполезна
- **Не гарантирует консистентность**: разрешения могут быть out-of-sync в распределённой системе
- **Не защищает от elevation of privilege bugs**: ошибки в коде могут обойти проверки

### 🌍 Реальные примеры

#### **Kafka: ACLs (Access Control Lists)**

Kafka использует ACL для контроля доступа к топикам, consumer groups, кластеру:

```
# Разрешить пользователю alice читать топик payments
kafka-acls --add \
  --allow-principal User:alice \
  --operation Read \
  --topic payments

# Разрешить сервису order-service писать в топик orders
kafka-acls --add \
  --allow-principal User:order-service \
  --operation Write \
  --topic orders

# Запретить всем delete топик
kafka-acls --add \
  --deny-principal User:* \
  --operation Delete \
  --topic orders
```

Структура ACL:
- **Principal**: кто (User:alice, Group:developers)
- **Operation**: что (Read, Write, Create, Delete, Describe, Alter)
- **Resource**: к чему (Topic:payments, Group:my-consumer-group, Cluster)
- **Allow/Deny**: разрешить или запретить

Важные нюансы:
- ACL проверяются для КАЖДОГО сообщения? Нет, только при подключении и subscribe
- Если ты можешь читать топик, ты можешь читать ВСЕ сообщения в нём (нет row-level security)
- ACL хранятся в Zookeeper (или KRaft metadata), синхронизируются на все брокеры

Типичная проблема:
- Ты дал сервису право читать топик `user-events`
- Разработчик создал новый топик `user-events-sensitive` с PII
- Сервис автоматически не получает доступ, но...
- Если ACL использует wildcard: `Topic:user-events*`, то доступ есть!
- Урок: будь осторожен с wildcards, используй prefix-based naming

Практический кейс:
- У тебя multi-tenant Kafka (несколько команд используют один кластер)
- Team A работает с топиками `team-a.*`
- Team B работает с топиками `team-b.*`
- Настрой ACL, чтобы Team A не могла читать топики Team B
- Используй principal groups: `Group:team-a` может работать только с `Topic:team-a*`

#### **PostgreSQL: GRANT/REVOKE и Row-Level Security**

**Table-level permissions**:
```sql
-- Дать пользователю alice право читать таблицу users
GRANT SELECT ON users TO alice;

-- Дать право писать, но не удалять
GRANT INSERT, UPDATE ON orders TO order_service;

-- Отозвать право
REVOKE DELETE ON orders FROM order_service;
```

**Row-Level Security (RLS)**:
```sql
-- Включить RLS на таблице
ALTER TABLE documents ENABLE ROW LEVEL SECURITY;

-- Политика: пользователь видит только свои документы
CREATE POLICY user_documents ON documents
  FOR SELECT
  USING (owner_id = current_user_id());

-- Политика: менеджеры видят документы своего отдела
CREATE POLICY manager_documents ON documents
  FOR SELECT
  USING (department_id IN (
    SELECT department_id FROM user_departments WHERE user_id = current_user_id()
  ));
```

Что происходит:
- Когда пользователь делает `SELECT * FROM documents`, Postgres автоматически добавляет `WHERE owner_id = current_user_id()`
- Пользователь физически не может получить чужие строки
- Даже если приложение забудет добавить фильтр, база защищена

Проблема производительности:
- RLS политики выполняются на КАЖДОМ запросе
- Если политика использует подзапросы, это может быть медленно
- Нужно индексировать столбцы, используемые в политиках

Инцидент (реальная ошибка):
- Разработчик включил RLS, но забыл создать политики
- По умолчанию RLS ЗАПРЕЩАЕТ весь доступ
- Приложение перестало видеть данные, все запросы возвращали пустые результаты
- Урок: тестируй авторизацию на разных ролях!

#### **MongoDB: Role-Based Access Control (RBAC)**

MongoDB использует роли для управления доступом:

**Встроенные роли**:
```javascript
// Роль только для чтения
db.createUser({
  user: "analytics",
  pwd: "password",
  roles: [{ role: "read", db: "production" }]
})

// Роль для чтения и записи
db.createUser({
  user: "app_service",
  pwd: "password",
  roles: [{ role: "readWrite", db: "production" }]
})

// Роль администратора базы
db.createUser({
  user: "dba",
  pwd: "password",
  roles: [{ role: "dbAdmin", db: "production" }]
})
```

**Кастомные роли**:
```javascript
// Роль: может читать коллекцию users, но только определённые поля
db.createRole({
  role: "limitedUserReader",
  privileges: [
    {
      resource: { db: "production", collection: "users" },
      actions: ["find"]
    }
  ],
  roles: []
})

// Применить роль к пользователю
db.grantRolesToUser("analytics", ["limitedUserReader"])
```

Field-level permissions (MongoDB Enterprise):
```javascript
// Разрешить читать только определённые поля
db.createRole({
  role: "publicUserReader",
  privileges: [
    {
      resource: { db: "production", collection: "users" },
      actions: ["find"],
      // Ограничение на поля (только в Enterprise)
      fields: ["username", "email", "created_at"]
    }
  ],
  roles: []
})
```

Проблема:
- В Community версии нет field-level security
- Пользователь с правом `find` на коллекцию видит ВСЕ поля
- Если в коллекции есть `password_hash`, он будет виден
- Решение: хранить чувствительные данные в отдельной коллекции

Типичная ошибка:
- Создал роль с правом `readWrite` на базу `production`
- Забыл, что это включает право создавать и удалять коллекции
- Баг в приложении случайно дропнул коллекцию
- Урок: используй более гранулярные права (только `insert`, `update`, `find`)

#### **RabbitMQ: Virtual Hosts и Permissions**

RabbitMQ использует концепцию **Virtual Hosts** (vhosts) для изоляции:

```bash
# Создать vhost
rabbitmqctl add_vhost /app-production
rabbitmqctl add_vhost /app-staging

# Создать пользователя
rabbitmqctl add_user app_service password123

# Дать права на vhost
rabbitmqctl set_permissions -p /app-production app_service ".*" ".*" ".*"
#                                                           conf  write read

# Права:
# conf:  создание/удаление exchanges и queues (regex)
# write: публикация сообщений
# read:  потребление сообщений
```

Гранулярные права:
```bash
# Разрешить создавать только queues с префиксом "service-a."
rabbitmqctl set_permissions -p /prod app_service "service-a\..*" ".*" ".*"

# Разрешить писать только в exchanges с префиксом "events."
rabbitmqctl set_permissions -p /prod publisher "" "events\..*" ""

# Разрешить читать только из queues с префиксом "consumer-x."
rabbitmqctl set_permissions -p /prod consumer "" "" "consumer-x\..*"
```

Практический кейс:
- У тебя несколько команд используют один RabbitMQ кластер
- Team A работает в vhost `/team-a`, Team B в `/team-b`
- Полная изоляция: Team A физически не может читать сообщения Team B
- Это проще и безопаснее, чем пытаться разграничить доступ через ACL

Проблема federation и shovel:
- RabbitMQ может пересылать сообщения между vhosts (shovel) или кластерами (federation)
- Если настроено неправильно, можно случайно "пробросить" данные из защищённого vhost в незащищённый
- Урок: тщательно проверяй конфигурацию shovel/federation

#### **ClickHouse: Row Policies и Quota**

ClickHouse поддерживает тонкие политики доступа:

**Row Policies**:
```sql
-- Создать политику: пользователь видит только данные своего региона
CREATE ROW POLICY regional_access ON events
  FOR SELECT
  USING region = currentUser()
  TO alice;

-- Политика для аналитиков: только данные старше 7 дней
CREATE ROW POLICY analytics_policy ON events
  FOR SELECT
  USING event_time < now() - INTERVAL 7 DAY
  TO analytics_team;
```

**Quota** (ограничение ресурсов):
```sql
-- Ограничить аналитиков: не больше 100 запросов в час
CREATE QUOTA analytics_quota
  FOR INTERVAL 1 HOUR MAX queries = 100
  TO analytics_team;

-- Ограничить объём читаемых данных
CREATE QUOTA data_limit
  FOR INTERVAL 1 DAY MAX result_rows = 1000000
  TO report_service;
```

Проблема с производительностью:
- Row policy добавляет `WHERE` условие к каждому запросу
- Если политика использует сложную функцию или подзапрос, запросы замедляются
- ClickHouse оптимизирует это (pushdown в WHERE), но не всегда эффективно

Инцидент:
- Разработчик создал row policy с `USING user_id = getCurrentUserId()`
- Функция `getCurrentUserId()` делала HTTP-запрос к auth сервису
- Каждый запрос к ClickHouse делал сетевой вызов
- Latency выросла в 100 раз, auth сервис упал под нагрузкой
- Урок: row policies должны использовать только быстрые, детерминированные функции

#### **Nginx: Location-based Access Control**

```nginx
# Только определённые IP могут обращаться к админке
location /admin {
    allow 10.0.0.0/8;      # внутренняя сеть
    allow 203.0.113.5;     # VPN IP
    deny all;               # все остальные
}

# Проверка токена в заголовке
location /api {
    if ($http_authorization != "Bearer secret-token") {
        return 403;
    }
    proxy_pass http://backend;
}

# Интеграция с auth_request
location /protected {
    auth_request /auth;
    proxy_pass http://backend;
}

location = /auth {
    internal;
    proxy_pass http://auth-service/verify;
    proxy_pass_request_body off;
    proxy_set_header Content-Length "";
    proxy_set_header X-Original-URI $request_uri;
}
```

Как работает `auth_request`:
1. Клиент делает запрос к `/protected/resource`
2. Nginx делает subrequest к `/auth` (auth-service)
3. Auth-service проверяет токен из заголовка, возвращает 200 или 403
4. Если 200, Nginx проксирует запрос в backend
5. Если 403, Nginx возвращает ошибку клиенту

Проблема:
- Auth-service становится single point of failure
- Если он недоступен, все запросы к `/protected` падают
- Решение: кеширование решений авторизации, fallback политики

### 💥 Типичные проблемы и инциденты

**1. Insecure Direct Object Reference (IDOR)**

Инцидент:
```
GET /api/orders/12345
```
- API проверяет, что пользователь аутентифицирован
- Но НЕ проверяет, что пользователь владеет заказом 12345
- Злоумышленник перебирает ID: `/api/orders/12346`, `/api/orders/12347`
- Получает доступ к чужим заказам

Урок: ВСЕГДА проверяй, что запрашиваемый ресурс принадлежит текущему пользователю.

**2. Horizontal Privilege Escalation**

Инцидент:
```
POST /api/users/123/change-email
{"email": "attacker@evil.com"}
```
- API проверяет, что пользователь залогинен
- Но не проверяет, что текущий пользователь — это user 123
- Злоумышленник меняет email другого пользователя
- Затем использует "восстановление пароля" для захвата аккаунта

**3. Vertical Privilege Escalation**

Инцидент:
```
POST /api/users/123/make-admin
```
- Endpoint существует, но не документирован
- Проверка роли забыта: `if (user.isAdmin) { ... }` отсутствует
- Обычный пользователь вызывает endpoint и становится администратором

Реальный случай (GitHub, 2020):
- Баг в GitHub Actions позволял получить write-доступ к любому репозиторию
- Проблема в проверке авторизации: GitHub проверял, что пользователь может читать репозиторий, но не проверял право на запись workflow

**4. Mass Assignment / Parameter Tampering**

Инцидент:
```json
POST /api/users
{
  "username": "alice",
  "email": "alice@example.com",
  "is_admin": true    // <- злоумышленник добавил это поле
}
```
- Бэкенд автоматически маппит все поля из JSON в модель User
- Поле `is_admin` не должно быть доступно для изменения, но проверки нет
- Пользователь создаётся с правами администратора

Урок: используй whitelisting полей, никогда не доверяй клиенту.

**5. Authorization Bypass через Cache**

Инцидент:
- Пользователь A запрашивает `/api/orders/12345`, получает 200 OK
- Nginx кеширует ответ
- Пользователь B (другой пользователь!) запрашивает тот же URL
- Nginx возвращает закешированный ответ, минуя проверку авторизации
- Пользователь B видит чужой заказ

Урок: не кешируй персонализированные данные без учёта идентификатора пользователя в cache key.

**6. Confused Deputy Problem**

Проблема:
- Микросервис A имеет высокие привилегии (может удалять пользователей)
- Микросервис B вызывает A от имени пользователя
- Микросервис A проверяет только свою аутентификацию, но не контекст вызова
- Результат: обычный пользователь через B может удалить любого пользователя

Решение: передавать оригинальный контекст (user principal) через весь call chain.

### 🛠 Практики проектирования

**1. Role-Based Access Control (RBAC)**
- Определи роли: `admin`, `user`, `read-only`
- Назначай разрешения ролям, а не конкретным пользователям
- Легко масштабируется: новый пользователь = назначение роли

**2. Attribute-Based Access Control (ABAC)**
- Более гибкий: принимай решения на основе атрибутов (время, локация, device type)
- Пример: "Разрешить доступ, если user.department == document.department И time < 18:00"
- Сложнее в реализации, но более выразительный

**3. Centralized Policy Engine**
- Open Policy Agent (OPA): единая точка для политик авторизации
- Политики описываются декларативно (язык Rego)
- Все сервисы запрашивают решения у OPA

**4. Defense in Depth**
- Проверяй авторизацию на КАЖДОМ уровне: API gateway, backend, база данных
- Даже если один уровень скомпрометирован, другие защищены

**5. Fail Securely**
- Если ошибка при проверке авторизации — запрети доступ (deny by default)
- Никогда не делай: `if (checkPermission()) { allow } catch { allow }`

**6. Audit All Authorization Decisions**
- Логируй все проверки авторизации (успешные и неудачные)
- Это критично для расследования инцидентов
- Включай: кто, что, когда, результат (allow/deny), причину

---

## 3. Trust Model Semantics (Семантика Моделей Доверия)

### 📋 Определение

**Trust Model Semantics** — это набор предположений о том, каким компонентам системы можно доверять, и какое поведение от них ожидается. Модель доверия определяет границы безопасности и то, какие угрозы система должна выдерживать.

```
       Доверенная зона                Недоверенная зона
┌──────────────────────────┐      ┌─────────────────────┐
│  Internal Services       │      │  External APIs      │
│  - Полное доверие        │<---->│  - Не доверяем      │
│  - Минимальная валидация │      │  - Полная валидация │
└──────────────────────────┘      └─────────────────────┘
```

### ✅ Что гарантирует

- **Trust Boundaries (границы доверия)**: явное определение, где заканчивается доверенная зона
- **Threat Model (модель угроз)**: понимание, какие атаки система должна выдерживать
- **Failure Assumptions (предположения о сбоях)**: какие типы сбоев возможны (crash vs Byzantine)
- **Security Perimeter (периметр безопасности)**: защита на границах доверия

### ❌ Что НЕ гарантирует

- **Не защищает от внутренних угроз**: если доверяешь компоненту, он может быть скомпрометирован
- **Не адаптируется автоматически**: модель доверия статична, не учитывает изменения в runtime
- **Не заменяет другие меры безопасности**: это абстракция для проектирования, не конкретная защита

### 🌍 Реальные примеры

#### **Традиционная модель: Perimeter Security**

Классический подход (до облаков и микросервисов):

```
Internet                    Firewall              Internal Network
   ║                           ║                         ║
   ║ <─────────────────────────╫─────────────────────────╣
   ║                           ║                         ║
   ║                      Недоверенная            Доверенная зона
   ║                         зона
```

Предположения:
- Всё **внутри** сети доверенно (internal services, databases, employees)
- Всё **снаружи** недоверенно (интернет, пользователи, партнёры)
- Защита на **периметре**: firewall, VPN, network segmentation

Как это выглядит в реальности (Kafka + PostgreSQL в приватной сети):

- Kafka кластер в приватном VPC (10.0.0.0/16)
- PostgreSQL в том же VPC
- Security группы разрешают трафик только внутри VPC
- Приложения деплоятся в том же VPC
- **Доверие**: раз трафик из VPC, значит это легитимный сервис
- **Проблема**: если один сервис скомпрометирован, он может атаковать все остальные

Пример:
- Web-сервер скомпрометирован через уязвимость
- Злоумышленник получает shell на сервере
- Оттуда может подключиться к Kafka БЕЗ аутентификации (т.к. Kafka доверяет всем в VPC)
- Читает все сообщения, включая чувствительные данные

Урок: perimeter security недостаточно в современных системах.

#### **MongoDB: Trust Internal Network**

MongoDB исторически проектировался для работы в доверенной сети:

До версии 3.6:
- По умолчанию MongoDB слушал на `0.0.0.0:27017` (все интерфейсы)
- Аутентификация отключена по умолчанию
- Предположение: MongoDB развёрнут в приватной сети, firewall блокирует внешний доступ

Что пошло не так:
- Многие разворачивали MongoDB в облаке без правильной настройки firewall
- Публичные IP с открытым портом 27017
- Боты сканировали интернет, находили открытые инстансы
- Ransomware атаки: "Мы удалили ваши данные, заплатите выкуп"

Инцидент (2017): более 27,000 MongoDB инстансов взломаны и очищены в течение недели.

Урок: **никогда** не полагайся только на network security. Всегда включай аутентификацию.

#### **Redis: Trusted Network Assumption**

Redis также исторически предполагал доверенную сеть:

Из документации Redis:
> "Redis is designed to be accessed by trusted clients inside trusted environments."

Что это значит:
- Нет аутентификации по умолчанию (до Redis 6.0 ACL)
- Нет TLS по умолчанию
- Некоторые команды могут быть опасны: `FLUSHALL`, `CONFIG SET`, `EVAL` (выполнение Lua)
- Команда `CONFIG SET dir /var/www/html` + `CONFIG SET dbfilename shell.php` + сохранение = RCE

Реальный инцидент:
- Redis развёрнут без пароля, доступен из интернета
- Злоумышленник использовал уязвимость для записи SSH ключа в `/root/.ssh/authorized_keys`
- Получил root доступ к серверу

Защита:
```
# redis.conf
bind 127.0.0.1          # слушать только на localhost
requirepass mypassword  # требовать пароль
rename-command CONFIG ""  # отключить опасные команды
```

#### **PostgreSQL: Trust Local Connections**

В `pg_hba.conf` можно настроить разные методы аутентификации для разных источников:

```
# TYPE  DATABASE    USER        ADDRESS         METHOD
local   all         all                         trust
host    all         all         127.0.0.1/32    trust
host    all         all         10.0.0.0/8      md5
host    all         all         0.0.0.0/0       reject
```

Что здесь происходит:
- `local` (Unix socket) и `127.0.0.1` (localhost) — метод `trust` (нет аутентификации)
- Предположение: если подключение локальное, значит это доверенный процесс
- `10.0.0.0/8` (приватная сеть) — требуется пароль (`md5`)
- Всё остальное — отклонить

Проблема:
- Если на сервере с Postgres запущен скомпрометированный процесс (например, web-сервер)
- Он может подключиться через localhost без пароля
- Получить полный доступ к базе

Лучшая практика:
```
local   all         postgres                    peer
local   all         all                         md5
host    all         all         127.0.0.1/32    scram-sha-256
```
- Даже локальные подключения требуют аутентификацию
- Только пользователь ОС `postgres` может подключаться как superuser через peer

#### **RabbitMQ: Default Guest User**

RabbitMQ имеет встроенного пользователя `guest/guest`:

До версии 3.3:
- Пользователь `guest` работал со всех хостов
- Предполагалось, что админ сразу сменит пароль

После версии 3.3:
- `guest` работает только с localhost
- Предположение: localhost — доверенная зона

Проблема:
- В Docker контейнерах "localhost" может быть доступен из других контейнеров в той же сети
- Если забыл удалить `guest` или создать нового пользователя, контейнеры могут получить доступ

Лучшая практика:
```bash
# Удалить guest
rabbitmqctl delete_user guest

# Создать пользователя для каждого приложения
rabbitmqctl add_user app_service strong-password
rabbitmqctl set_permissions -p /prod app_service ".*" ".*" ".*"
```

#### **Nginx: Trust Backend Servers**

Типичная конфигурация:

```nginx
location / {
    proxy_pass http://backend;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

Предположение:
- Nginx — это entry point, проверяет авторизацию
- Backend доверяет заголовкам от Nginx (`X-Real-IP`)
- Backend не делает повторную проверку авторизации

Проблема:
- Если злоумышленник получает прямой доступ к backend (минуя Nginx)
- Может подделать заголовок `X-Real-IP: 10.0.0.1` (admin IP)
- Backend доверяет этому заголовку и даёт доступ

Решение:
```nginx
# На backend: разрешить подключения только от Nginx
# iptables или security groups

# На backend в коде:
# ВСЕГДА проверяй, что запрос пришёл от reverse proxy
# Не доверяй заголовкам от внешних клиентов
```

### 💥 Типичные проблемы и инциденты

**1. Lateral Movement после компрометации**

Инцидент (Target breach, 2013):
- Хакеры скомпрометировали HVAC подрядчика (система кондиционирования)
- У подрядчика был VPN доступ к сети Target для мониторинга систем
- Из этой точки хакеры двигались laterally по внутренней сети
- Дошли до POS (point-of-sale) систем
- Украли 40 миллионов номеров кредитных карт

Проблема: Target доверял всем в внутренней сети. HVAC подрядчику не нужен был доступ к платёжным системам.

Урок: сегментация сети, минимальные привилегии, не доверяй даже внутренним системам.

**2. SSRF (Server-Side Request Forgery)**

Инцидент:
- У тебя есть фича: "Загрузить изображение по URL"
- Код: `fetch(user_provided_url)` выполняется на сервере
- Злоумышленник отправляет URL: `http://169.254.169.254/latest/meta-data/iam/security-credentials/`
- Это AWS metadata endpoint, доступен только с EC2 инстанса
- Сервер доверяет сам себе, возвращает AWS credentials
- Злоумышленник получает ключи от AWS аккаунта

Проблема: сервер доверяет запросам, исходящим от себя.

Решение: whitelist разрешённых доменов, блокировать приватные IP, не доверяй user input.

**3. Trusting User Input**

Классика:
```sql
-- Небезопасный код (SQL injection)
query = "SELECT * FROM users WHERE username = '" + user_input + "'"
```

Пользователь вводит: `admin' OR '1'='1`

Запрос становится: `SELECT * FROM users WHERE username = 'admin' OR '1'='1'`

Возвращает ВСЕХ пользователей.

Проблема: доверие пользовательским данным.

Решение: prepared statements, никогда не конкатенируй SQL.

### 🛠 Практики проектирования

**1. Network Segmentation**
- Разделяй сеть на зоны: DMZ (public), Application tier, Data tier
- Между зонами — firewalls с минимальными правилами
- Даже если одна зона скомпрометирована, другие защищены

**2. Principle of Least Privilege**
- Давай минимальные разрешения, необходимые для работы
- Сервису для чтения метрик не нужен write доступ к production базе

**3. Assume Breach**
- Проектируй систему, предполагая, что какой-то компонент уже скомпрометирован
- Как ограничить ущерб? Как обнаружить атаку?

**4. Trust Verification**
- Если принимаешь данные от "доверенного" компонента, всё равно валидируй
- Даже если заголовок пришёл от Nginx, проверь его корректность

**5. Explicit Trust Boundaries**
- Документируй, где проходят границы доверия
- На границах — строгая валидация, аутентификация, авторизация

---

## 4. Zero-Trust Semantics (Семантика Нулевого Доверия)

### 📋 Определение

**Zero-Trust Semantics** — это модель безопасности, основанная на принципе "never trust, always verify" (никогда не доверяй, всегда проверяй). В отличие от perimeter security, zero-trust предполагает, что угроза может исходить откуда угодно — как извне, так и изнутри сети.

```
Традиционная модель:              Zero-Trust модель:
┌──────────────────┐              ┌──────────────────┐
│   Trusted Zone   │              │  Service A       │
│  ┌────┐  ┌────┐  │              │  [Auth ✓]        │
│  │ S1 │──│ S2 │  │              └────────┬─────────┘
│  └────┘  └────┘  │                       │ mTLS + authz
│   (no checks)    │                       ▼
└──────────────────┘              ┌──────────────────┐
                                  │  Service B       │
                                  │  [Auth ✓]        │
                                  └──────────────────┘
```

Ключевые принципы:
1. **Verify explicitly**: проверяй каждый запрос, даже от внутренних сервисов
2. **Least privilege access**: минимальные разрешения на каждом уровне
3. **Assume breach**: предполагай, что система уже скомпрометирована

### ✅ Что гарантирует

- **End-to-End Authentication**: каждый запрос аутентифицирован, независимо от источника
- **Granular Authorization**: проверка разрешений на каждом хопе
- **Encrypted Communication**: все данные в транзите зашифрованы (mTLS)
- **Continuous Verification**: постоянная проверка, а не одноразовая при входе в сеть
- **Minimal Blast Radius**: если один компонент скомпрометирован, ущерб ограничен

### ❌ Что НЕ гарантирует

- **Не устраняет все риски**: zero-trust — это не серебряная пуля, а один из слоёв защиты
- **Не защищает от логических уязвимостей**: если в коде есть баг, zero-trust его не исправит
- **Не заменяет другие практики**: всё ещё нужны патчи, мониторинг, incident response
- **Не бесплатно**: добавляет сложность, latency, операционные издержки

### 🌍 Реальные примеры

#### **Google BeyondCorp**

Google внедрил zero-trust модель после операции Aurora (2010), когда хакеры проникли во внутреннюю сеть.

Старая модель:
- VPN для доступа к внутренним сервисам
- Попал в VPN = доступ ко всему

Новая модель (BeyondCorp):
- Нет VPN, нет "trusted network"
- Каждый сервис доступен из интернета (с аутентификацией)
- Каждый запрос проверяется:
  - Кто делает запрос? (user identity)
  - С какого устройства? (device trust level)
  - Что запрашивается? (resource)
  - Разрешено ли? (policy engine)

Пример:
- Сотрудник пытается открыть внутреннюю админку из кафе
- Access Proxy проверяет:
  - JWT токен (аутентификация пользователя)
  - Device certificate (устройство управляемое IT)
  - Device health (патчи установлены, антивирус активен)
  - Policy: "Сотрудник из отдела X может обращаться к админке только с корпоративного устройства"
- Если всё ОК — пропустить, иначе — заблокировать

#### **Service Mesh: Istio, Linkerd**

Service mesh реализует zero-trust для микросервисов:

**Mutual TLS (mTLS) по умолчанию**:
```yaml
# Istio: включить mTLS для всех сервисов
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
spec:
  mtls:
    mode: STRICT
```

Что происходит:
- Каждый pod получает уникальный сертификат (от Istio CA)
- Любое соединение между сервисами шифруется с mTLS
- Сервис A знает, что говорит именно с сервисом B (а не с подделкой)
- Автоматическая ротация сертификатов (каждые 24 часа)

**Fine-grained Authorization**:
```yaml
# Разрешить только сервису frontend вызывать backend
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: backend-policy
spec:
  selector:
    matchLabels:
      app: backend
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/default/sa/frontend"]
    to:
    - operation:
        methods: ["GET", "POST"]
```

Преимущества:
- Разработчики не пишут код для mTLS — это делает service mesh
- Политики centralized, легко аудировать
- Если pod скомпрометирован, он не может выдать себя за другой сервис (нет сертификата)

#### **Kafka с mTLS и ACLs**

Zero-trust для Kafka означает:

1. **mTLS между клиентами и брокерами**:
```properties
# Producer config
security.protocol=SSL
ssl.truststore.location=/path/to/truststore.jks
ssl.keystore.location=/path/to/keystore.jks
ssl.keystore.password=password
```

- Каждый producer/consumer имеет уникальный сертификат
- Брокер проверяет сертификат клиента при подключении
- Клиент проверяет сертификат брокера (защита от MITM)

2. **ACLs для каждого principal**:
```bash
# Разрешить сервису payment-service писать только в топик payments
kafka-acls --add \
  --allow-principal User:CN=payment-service \
  --operation Write \
  --topic payments
```

3. **mTLS между брокерами**:
```properties
# server.properties
security.inter.broker.protocol=SSL
```

- Даже репликация между брокерами идёт через mTLS
- Защита от rogue broker (поддельный брокер не сможет подключиться)

Практический кейс:
- У тебя 50 микросервисов, каждый производит и потребляет события
- Без zero-trust: любой сервис может читать любой топик (если знает имя)
- С zero-trust:
  - Каждый сервис имеет уникальный сертификат
  - ACLs ограничивают доступ: payment-service может писать только в `payments`, читать только из `orders`
  - Если сервис скомпрометирован, злоумышленник не может читать топики других команд

#### **PostgreSQL: Always Require SSL + Role-Based Access**

```
# pg_hba.conf — требовать SSL для всех подключений
hostssl  all  all  0.0.0.0/0  scram-sha-256
```

- Любое подключение должно быть через SSL
- Даже из той же приватной сети
- Защита от перехвата трафика (man-in-the-middle)

**Certificate-based authentication для сервисов**:
```
hostssl  mydb  payment_service  0.0.0.0/0  cert
```

- Сервис `payment_service` аутентифицируется сертификатом, а не паролем
- Даже если кто-то перехватил трафик, он не получит credentials (нет пароля)
- Автоматическая ротация сертификатов через secret management (Vault, cert-manager)

**Row-Level Security для каждого сервиса**:
```sql
CREATE POLICY payment_service_policy ON orders
  FOR SELECT
  USING (service_id = current_setting('app.service_id'));
```

- Даже если сервис имеет доступ к таблице, он видит только свои строки
- Defense in depth: если ACL пробит, RLS всё ещё защищает

#### **ClickHouse: User Quotas + Encryption**

```xml
<!-- config.xml -->
<clickhouse>
    <openSSL>
        <server>
            <certificateFile>/path/to/server.crt</certificateFile>
            <privateKeyFile>/path/to/server.key</privateKeyFile>
            <caConfig>/path/to/ca.crt</caConfig>
            <verificationMode>strict</verificationMode>
        </server>
    </openSSL>
</clickhouse>
```

- Все подключения через TLS
- Можно включить client certificate verification

**Quota для каждого пользователя**:
```sql
CREATE QUOTA analytics_quota
  FOR INTERVAL 1 HOUR MAX queries = 100, MAX result_rows = 1000000
  TO analytics_user;
```

- Даже если учётная запись скомпрометирована, злоумышленник ограничен в ресурсах
- Не может "высосать" всю базу за один запрос

#### **Nginx: Always Verify Backend Identity**

```nginx
# Проксирование на backend с проверкой TLS сертификата
location /api {
    proxy_pass https://backend.internal;
    proxy_ssl_verify on;
    proxy_ssl_trusted_certificate /etc/nginx/ca.crt;
    proxy_ssl_name backend.internal;
}
```

- Nginx проверяет, что backend имеет валидный сертификат
- Защита от DNS spoofing: даже если DNS скомпрометирован и `backend.internal` указывает на evil сервер, Nginx не примет невалидный сертификат

### 💥 Типичные проблемы и инциденты

**1. Partial Implementation**

Проблема:
- Внедрили mTLS для 80% сервисов, но оставили несколько "legacy" без защиты
- Злоумышленник нашёл незащищённый сервис
- Оттуда двигался lateral по сети

Урок: zero-trust должен быть везде, иначе это просто дырявый perimeter.

**2. Certificate Management Overhead**

Проблема:
- Внедрили mTLS, но забыли про ротацию сертификатов
- Сертификаты истекли
- Все сервисы перестали общаться друг с другом
- Downtime

Решение: автоматизация выпуска и ротации (cert-manager, Vault, AWS ACM).

**3. Performance Impact**

Проблема:
- Включили mTLS для всех соединений
- TLS handshake добавил latency
- Throughput упал на 20%

Решение: TLS session resumption, connection pooling, оптимизация crypto (hardware acceleration).

**4. Overly Permissive Policies**

Проблема:
- Написали Authorization Policy: "Разрешить всем сервисам с лейблом `app=*` вызывать API"
- Это по сути всё равно что "разрешить всем"

Урок: политики должны быть максимально гранулярными. Используй explicit allow list, а не wildcards.

**5. Trusting Identity Providers**

Проблема:
- Используешь JWT токены для аутентификации
- Проверяешь подпись, всё ОК
- Но не проверяешь `iss` (issuer) — кто выпустил токен
- Злоумышленник создаёт свой auth сервис, выпускает токены для любого пользователя
- Твой сервис принимает их (подпись валидна)

Решение: всегда проверяй `iss`, используй whitelist разрешённых issuers.

### 🛠 Практики проектирования

**1. Identity для всего**
- Каждый сервис, pod, пользователь должен иметь идентичность (сертификат, токен, principal)
- Нет анонимных запросов

**2. Mutual TLS везде**
- Не только клиент проверяет сервер, но и сервер проверяет клиента
- Service mesh упрощает внедрение

**3. Policy-as-Code**
- Политики авторизации в version control (Git)
- Изменения через code review
- Audit trail для всех изменений

**4. Continuous Monitoring**
- Логируй все решения авторизации
- Детектируй аномалии (необычные пути доступа, новые principals)
- Автоматические алерты

**5. Defense in Depth**
- Zero-trust — это не замена firewall, а дополнение
- Комбинируй: network segmentation + mTLS + authorization + encryption

**6. Automate Certificate Management**
- Сертификаты с коротким TTL (часы/дни, не годы)
- Автоматическая ротация (cert-manager, SPIFFE/SPIRE)
- Мониторинг истечения сертификатов

---

## 5. Byzantine Trust Assumptions (Византийские Предположения о Доверии)

### 📋 Определение

**Byzantine Trust Assumptions** — это модель, в которой система должна работать корректно, даже если некоторые участники ведут себя **злонамеренно** (не просто падают, а активно саботируют систему: лгут, подделывают сообщения, координируются с другими злоумышленниками).

Термин происходит от **Byzantine Generals Problem** (проблема византийских генералов):

```
Представь армию, окружившую город. Генералы должны координировать атаку.
Но среди них есть предатели, которые отправляют противоречивые приказы:
- Генерал A говорит B: "Атакуем на рассвете"
- Предатель C говорит B от имени A: "Отступаем на рассвете"

Задача: достичь консенсуса, даже если до f генералов — предатели.
```

### Типы сбоев

| Тип сбоя | Описание | Пример |
|----------|----------|--------|
| **Crash Fault** | Узел падает и перестаёт отвечать | Сервер выключился, процесс убит OOM killer |
| **Omission Fault** | Узел пропускает сообщения (не отправляет/не получает) | Сетевой partition, packet loss |
| **Timing Fault** | Узел отвечает слишком медленно или слишком быстро | Clock drift, GC pause |
| **Byzantine Fault** | Узел ведёт себя произвольно и злонамеренно | Скомпрометирован хакерами, присылает неверные данные, подделывает подписи |

**Byzantine Fault Tolerance (BFT)** — способность системы работать корректно при наличии византийских сбоев.

### ✅ Что гарантирует

- **Safety**: система не примет неверное решение, даже если f узлов злонамеренны
- **Liveness**: система продолжит работу (при достаточном количестве честных узлов)
- **Consistency**: все честные узлы видят одинаковое состояние
- **Agreement**: честные узлы достигнут консенсуса, несмотря на византийских участников

**Теорема**: система из `n` узлов может выдержать `f` византийских сбоев, если `n ≥ 3f + 1`.

Пример:
- 4 узла → может выдержать 1 византийский сбой
- 7 узлов → может выдержать 2 византийских сбоя
- 10 узлов → может выдержать 3 византийских сбоя

### ❌ Что НЕ гарантирует

- **Не защищает от большинства злоумышленников**: если больше f узлов скомпрометированы, система может быть атакована
- **Не защищает от Sybil attack**: если злоумышленник может создавать неограниченное количество узлов
- **Не гарантирует производительность**: BFT алгоритмы значительно медленнее CFT (Crash Fault Tolerant)
- **Не заменяет аутентификацию**: всё ещё нужно проверять, что сообщения от легитимных узлов

### 🌍 Реальные примеры

#### **Blockchain: Bitcoin, Ethereum**

Bitcoin — это классический пример системы с Byzantine Fault Tolerance.

Проблема:
- Тысячи узлов по всему миру
- Нет центрального авторитета
- Некоторые узлы могут быть злонамеренными (пытаются создать фальшивые транзакции)

Решение (Proof-of-Work):
- Чтобы добавить блок, нужно решить криптографическую задачу (майнинг)
- Это дорого (электричество, оборудование)
- Атаковать систему дороже, чем играть честно

Гарантии:
- Даже если 49% узлов злонамеренны, система работает корректно
- Но если 51% скомпрометированы (51% attack), можно переписать историю

Практический кейс:
- В 2014 году майнинг-пул GHash.IO контролировал >50% хешрейта Bitcoin
- Теоретически мог провести double-spend атаку
- Сообщество паниковало, майнеры добровольно ушли из пула
- Урок: важна децентрализация, нельзя допускать концентрации power

#### **Kafka: NOT Byzantine Fault Tolerant**

Kafka предполагает **crash-fault tolerance**, но НЕ byzantine.

Предположения:
- Брокеры могут упасть (crash), но не будут злонамеренно врать
- Брокеры могут потерять связь (partition), но не будут подделывать сообщения
- Producers/Consumers доверенные (после аутентификации)

Что может пойти не так:
- Скомпрометированный брокер может:
  - Подделать offset (consumers будут пропускать сообщения)
  - Изменить содержимое сообщений
  - Удалить сегменты логов
- Kafka НЕ проверяет криптографические подписи сообщений
- Kafka НЕ имеет механизма для обнаружения подделки данных

Защита:
- TLS + mTLS: шифрование в транзите, проверка идентичности
- ACLs: ограничение, кто может писать в топик
- Audit logs: обнаружение подозрительных действий
- НО: если брокер скомпрометирован, он может обойти всё это изнутри

Когда нужен BFT для Kafka:
- Если брокеры управляются разными организациями, которые не доверяют друг другу
- Решение: использовать blockchain или BFT-систему для critical events

#### **PostgreSQL: Replication Trust**

PostgreSQL streaming replication предполагает доверие между master и replica:

```
# На master
host  replication  replicator  10.0.0.0/8  trust
```

Проблема:
- Если replica скомпрометирована, она может отправить вредоносные запросы к master
- Если master скомпрометирован, он может отправить поддельные данные на replicas

PostgreSQL НЕ имеет:
- Криптографических подписей для WAL (Write-Ahead Log)
- Механизма проверки, что данные от master валидны
- Защиты от Byzantine master (который намеренно портит данные)

Практический кейс:
- У тебя Multi-Master репликация (через BDR или Citus)
- Один из master скомпрометирован
- Он начинает отправлять SQL injection в репликационный поток
- Другие masters применяют эти изменения
- База данных скомпрометирована

Защита:
- Используй только доверенные узлы для репликации
- Network segmentation: replicas в изолированной сети
- Мониторинг: детектируй аномалии в репликационном лаге, объёме данных

#### **ClickHouse: Replication через ZooKeeper**

ClickHouse использует ZooKeeper для координации репликации:

```sql
CREATE TABLE events (
    event_time DateTime,
    user_id UInt32,
    event_type String
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/events', '{replica}')
PARTITION BY toYYYYMM(event_time)
ORDER BY (user_id, event_time);
```

Как это работает:
- Каждая реплика регистрируется в ZooKeeper
- Когда данные вставляются в реплику 1, она записывает метаданные в ZooKeeper
- Реплика 2 видит новую запись в ZooKeeper, скачивает данные от реплики 1
- ZooKeeper используется для выбора leader для partition

Проблема:
- Если ZooKeeper скомпрометирован, он может:
  - Отправить реплике 2 неверный адрес (скачать данные от evil сервера)
  - Указать неверный leader
  - Подделать метаданные (checksum, количество строк)
- ClickHouse НЕ проверяет криптографические подписи данных

Защита:
- ZooKeeper в доверенной зоне, изолированный доступ
- mTLS для соединений между ClickHouse и ZooKeeper
- Checksums для data parts (но не криптографические подписи)

#### **RabbitMQ: Quorum Queues**

RabbitMQ quorum queues используют Raft для репликации:

```bash
rabbitmqctl declare queue name=my-queue durable=true \
  arguments='{"x-queue-type": "quorum"}'
```

Raft предполагает **Crash Fault Tolerance**, не Byzantine:
- Узлы могут упасть, но не будут врать
- Если leader падает, новый leader выбирается голосованием
- Majority (3 из 5) должны согласиться на каждую операцию

Что может пойти не так:
- Скомпрометированный узел может:
  - Отправить неверные votes (проголосовать за два разных leader)
  - Подделать log entries
  - Изменить содержимое сообщений
- Raft НЕ имеет защиты от этого (предполагает честные узлы)

Когда это проблема:
- Если RabbitMQ развёрнут в multi-cloud (AWS + GCP + Azure)
- Если каждый cloud provider управляет своими узлами
- Они могут не доверять друг другу

Решение:
- Используй BFT-протокол (PBFT, Tendermint) вместо Raft
- Добавь криптографические подписи для сообщений

#### **Redis Sentinel: NOT Byzantine Fault Tolerant**

Redis Sentinel используется для автоматического failover:

```
Sentinel 1, 2, 3 мониторят Master
Если Master падает, Sentinels голосуют за новый Master
Majority (2 из 3) должны согласиться
```

Проблема:
- Если Sentinel скомпрометирован, он может:
  - Заявить, что Master упал (хотя он работает)
  - Проголосовать за себя как за нового Master
  - Переключить трафик на evil Redis

Redis НЕ имеет защиты от Byzantine Sentinels.

Практический кейс:
- Sentinel 1 скомпрометирован
- Он отправляет fake heartbeats от имени Master
- Sentinel 2 и 3 думают, что Master работает
- На самом деле Master упал
- Failover не происходит, downtime

Защита:
- Запускай Sentinels на разных physical hosts (не виртуалках на одном сервере)
- Network segmentation
- Мониторинг: алерты на split-brain

### 💥 Типичные проблемы и инциденты

**1. 51% Attack на блокчейны**

Инцидент (Ethereum Classic, 2020):
- Злоумышленник арендовал достаточно hashpower для контроля >51% сети
- Провёл double-spend атаку
- Отправил $5.6M в ETC на биржу, вывел в другую криптовалюту
- Затем переписал блокчейн, откатив свои транзакции
- Украл монеты, биржа потеряла деньги

Урок: даже BFT системы уязвимы, если злоумышленник контролирует больше f узлов.

**2. BGP Hijacking**

Инцидент (MyEtherWallet, 2018):
- Злоумышленники скомпрометировали BGP (Border Gateway Protocol)
- Перенаправили трафик с myetherwallet.com на свой сервер
- Пользователи вводили private keys, думая что это легитимный сайт
- Украдено $150,000

Проблема: BGP НЕ имеет Byzantine Fault Tolerance. Любой AS (Autonomous System) может анонсировать любой IP prefix.

Решение: RPKI (Resource Public Key Infrastructure) — криптографические подписи для BGP routes.

**3. Compromised NTP Server**

Проблема:
- Многие системы полагаются на NTP для синхронизации времени
- Если NTP сервер скомпрометирован, он может отправлять неверное время
- Это может сломать:
  - Certificate validation (expired/not yet valid)
  - Kerberos authentication (time-sensitive)
  - Consensus algorithms (timeouts)
  - Audit logs (неверные timestamps)

Решение: используй несколько NTP серверов (pool.ntp.org), детектируй outliers.

**4. Sybil Attack**

Проблема:
- Система использует голосование для принятия решений
- Злоумышленник создаёт множество fake identities
- Контролирует majority голосов

Пример:
- P2P сеть для file sharing
- Репутация узлов основана на голосовании
- Злоумышленник создаёт 1000 fake узлов
- Все голосуют за evil peer, который раздаёт вредоносные файлы

Защита: Proof-of-Work (дорого создавать узлы), Proof-of-Stake (нужно вложить деньги), централизованная регистрация (trusted authority).

### 🛠 Практики проектирования

**1. Используй BFT только когда нужно**
- BFT значительно дороже (latency, throughput, сложность)
- Если все узлы доверенные, используй Crash Fault Tolerant (Raft, Paxos)
- BFT нужен для: blockchain, multi-organization systems, untrusted networks

**2. Cryptographic Signatures**
- Каждое сообщение должно быть подписано отправителем
- Получатель проверяет подпись
- Это защищает от подделки сообщений

**3. Majority Voting**
- Не доверяй одному узлу
- Запрашивай данные от нескольких узлов, сравнивай результаты
- Используй majority (2f+1 из 3f+1)

**4. Threshold Cryptography**
- Распредели секрет между несколькими узлами (Shamir's Secret Sharing)
- Чтобы восстановить секрет, нужно f+1 долей
- Даже если f узлов скомпрометированы, секрет безопасен

**5. Monitoring и Anomaly Detection**
- Логируй все действия узлов
- Детектируй аномалии: узел отправляет противоречивые сообщения, узел игнорирует запросы
- Автоматический ban подозрительных узлов

**6. Redundancy и Diversity**
- Используй узлы от разных провайдеров (AWS, GCP, Azure)
- Разные ОС, разные версии софта
- Это защищает от скоординированной атаки (компрометация одного провайдера)

**7. Economic Incentives**
- Сделай атаку дороже, чем честное поведение
- Proof-of-Stake: атакующий потеряет stake
- Reputation systems: злонамеренные узлы теряют репутацию

---

## Сравнительная таблица

| Характеристика | Authentication | Authorization | Trust Model | Zero-Trust | Byzantine Trust |
|----------------|----------------|---------------|-------------|------------|-----------------|
| **Основной вопрос** | Кто ты? | Что тебе разрешено? | Кому можно доверять? | Никому нельзя доверять | Кто может вести себя злонамеренно? |
| **Scope** | Identity verification | Access control | Trust boundaries | Continuous verification | Fault tolerance |
| **Предположение** | Credentials = identity | Identity → permissions | Trusted vs untrusted zones | All zones untrusted | Some nodes malicious |
| **Механизмы** | Passwords, certs, tokens | ACL, RBAC, ABAC | Firewall, VPN, segmentation | mTLS, policy engine | BFT consensus, signatures |
| **Performance Impact** | Low (one-time) | Low-Medium (per request) | Low | Medium-High (crypto overhead) | Very High (consensus, voting) |
| **Complexity** | Low | Medium | Low-Medium | High | Very High |
| **Failure Mode** | Credential theft | Privilege escalation | Lateral movement | Certificate expiry | >f nodes compromised |
| **Use Cases** | Login, API keys | File access, API endpoints | Enterprise networks | Service mesh, cloud | Blockchain, multi-org |
| **Kafka Support** | SASL, mTLS | ACLs | VPC, network policies | mTLS + ACLs | NO (crash-tolerant only) |
| **Postgres Support** | Password, cert | GRANT, RLS | pg_hba.conf zones | SSL + RLS | NO (replication trusts master) |
| **Redis Support** | AUTH, ACL (6.0+) | ACL per key pattern | bind to localhost | TLS + ACL | NO |
| **MongoDB Support** | SCRAM, x.509 | RBAC | Network segmentation | TLS + RBAC | NO |
| **RabbitMQ Support** | Username/password | Vhost permissions | Vhost isolation | TLS + permissions | NO (Raft is CFT) |

---

## Комбинации семантик

В реальных системах семантики комбинируются для достижения требуемого уровня безопасности:

### 1. **Authentication + Authorization** (базовая комбинация)

Это минимум для любой production системы:

```
Request → [Authentication] → [Authorization] → [Execute]
          ↓                  ↓
          "Кто ты?"         "Разрешено ли тебе?"
```

Пример (Kafka):
- SASL/SCRAM для аутентификации клиента
- ACL для проверки прав на топик

Без одного из компонентов:
- Только Authentication: все залогиненные могут делать всё
- Только Authorization: кто угодно может притвориться кем угодно

### 2. **Trust Model + Authentication + Authorization**

Классическая perimeter security:

```
┌─────────────────────────────────────┐
│  Trusted Zone (Internal Network)    │
│  ┌───────────────────────────────┐  │
│  │ Service A                     │  │
│  │ Auth: Internal JWT            │  │
│  │ Authz: Relaxed (same network) │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
         ▲
         │ Firewall
         │ Strict auth + authz
         ▼
┌─────────────────────────────────────┐
│  Untrusted Zone (Internet)          │
│  - Requires strong auth (mTLS)      │
│  - Fine-grained authz               │
└─────────────────────────────────────┘
```

Пример:
- Postgres: `pg_hba.conf` с `trust` для localhost, `scram-sha-256` для external
- Redis: bind только на 127.0.0.1 для local apps, require password для remote

### 3. **Zero-Trust = Authentication + Authorization + NO Trust Model**

Каждый запрос проверяется, независимо от источника:

```
Service A --[mTLS]--┐
                     ├--[Policy Engine]-- Service C
Service B --[mTLS]--┘
         ↓
    Каждый запрос:
    - Аутентификация (проверка сертификата)
    - Авторизация (проверка policy)
```

Пример (Istio):
```yaml
# Authentication: требовать mTLS
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
spec:
  mtls:
    mode: STRICT

# Authorization: только frontend может вызывать backend
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: backend-policy
spec:
  selector:
    matchLabels:
      app: backend
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/default/sa/frontend"]
```

### 4. **Byzantine Trust + Authentication + Authorization**

Для систем с недоверенными участниками:

```
Node A --[signed msg]--┐
                        ├--[BFT Consensus]-- Decision
Node B --[signed msg]--┤    (2f+1 agree)
                        │
Node C --[signed msg]--┘

Каждый узел:
- Аутентификация: проверка подписи сообщения
- Авторизация: проверка, что отправитель имеет право участвовать
- Byzantine tolerance: majority voting, нет доверия отдельным узлам
```

Пример (Blockchain):
- Miners аутентифицируются криптографическими ключами
- Авторизация: только валидные транзакции (достаточный баланс, верная подпись)
- Byzantine tolerance: majority hash power определяет canonical chain

### 5. **Hybrid: Trusted Core + Zero-Trust Edges**

Практичная модель для enterprise:

```
┌────────────────────────────────────────┐
│  Core Services (Trusted)               │
│  - Database cluster                    │
│  - Internal message bus                │
│  - Relaxed auth (certificates)         │
└────────────┬───────────────────────────┘
             │
    ┌────────┴────────┐
    │  API Gateway    │ ← Zero-Trust boundary
    │  (Zero-Trust)   │
    └────────┬────────┘
             │
    ┌────────┴────────────────────┐
    │  External Services/Users    │
    │  - Strict auth (OAuth)      │
    │  - Fine-grained authz       │
    │  - Rate limiting            │
    └─────────────────────────────┘
```

Преимущества:
- Core optimized для performance (меньше overhead)
- Edges защищены zero-trust
- Балансирует безопасность и производительность

---

## Практические рекомендации

### Для небольших систем (стартап, MVP)

**Минимальный набор**:
1. Authentication везде (даже на dev инстансах)
   - Kafka: включи SASL
   - Postgres: убери `trust` из `pg_hba.conf`
   - Redis: установи `requirepass`
   - MongoDB: создай пользователей с паролями

2. Authorization на уровне приложения
   - RBAC: роли `admin`, `user`, `read-only`
   - Проверяй принадлежность ресурсов пользователю (IDOR protection)

3. TLS для данных в транзите
   - Особенно важно для баз данных и message queues
   - Let's Encrypt для веб-сервисов (бесплатно)

4. Trust model: perimeter security
   - Firewall: разрешить только необходимые порты
   - VPC/private network для внутренних сервисов

**Чего избегать**:
- Zero-trust (слишком сложно для маленькой команды)
- Byzantine fault tolerance (не нужно, если все узлы твои)

### Для средних систем (растущий бизнес)

**Добавить**:
1. Centralized authentication
   - OAuth 2.0 / OIDC
   - Единая точка для управления пользователями

2. Fine-grained authorization
   - Database: row-level security
   - Kafka: ACLs per topic
   - MongoDB: роли с минимальными привилегиями

3. Secrets management
   - Vault, AWS Secrets Manager
   - Автоматическая ротация credentials

4. Audit logging
   - Логировать все действия пользователей
   - SIEM для анализа логов

5. Network segmentation
   - Разные VPC/subnets для разных tier (web, app, data)
   - Минимальные firewall rules между ними

### Для больших систем (enterprise, fintech)

**Добавить**:
1. Zero-Trust architecture
   - Service mesh (Istio, Linkerd)
   - mTLS для всех inter-service communications
   - Policy engine (OPA)

2. Defense in Depth
   - Проверка авторизации на каждом уровне (gateway, service, database)
   - WAF (Web Application Firewall)
   - DDoS protection

3. Anomaly detection
   - Machine learning для обнаружения необычных паттернов доступа
   - Автоматический блок подозрительных IP/пользователей

4. Incident Response
   - Playbooks для типичных инцидентов
   - Автоматическое отключение скомпрометированных сервисов
   - Backup и disaster recovery с RTO < 1 час

5. Compliance
   - SOC 2, ISO 27001, PCI DSS (в зависимости от индустрии)
   - Regular security audits и penetration testing

### Для multi-organization систем (партнёрства, блокчейн)

**Добавить**:
1. Byzantine Fault Tolerance
   - Используй BFT consensus (PBFT, Tendermint)
   - Криптографические подписи для всех сообщений

2. Zero-Knowledge Proofs
   - Для sharing данных без раскрытия деталей
   - Homomorphic encryption

3. Smart Contracts
   - Автоматическое выполнение соглашений
   - Immutable audit trail

4. Federated Identity
   - Каждая организация управляет своими пользователями
   - Trust federation для cross-org доступа

---

## Типичные ошибки и как их избежать

### 1. "Security by Obscurity"

**Ошибка**:
```
"Наша база данных доступна на порту 33333 (не стандартном 5432),
поэтому хакеры её не найдут. Пароль не нужен."
```

**Почему это плохо**:
- Port scanners найдут открытый порт за секунды
- Obscurity не заменяет настоящую безопасность

**Как правильно**:
- Используй аутентификацию ВСЕГДА
- Obscurity как дополнительный слой, не единственный

### 2. Хранение secrets в коде

**Ошибка**:
```python
# config.py
KAFKA_PASSWORD = "super-secret-123"
```

**Почему это плохо**:
- Попадает в Git
- Видно всем разработчикам
- Сложно ротировать

**Как правильно**:
```python
import os
KAFKA_PASSWORD = os.environ['KAFKA_PASSWORD']
```
- Используй environment variables
- Vault, AWS Secrets Manager для хранения
- Разные secrets для dev/staging/prod

### 3. Переиспользование credentials

**Ошибка**:
```
Один API ключ для всех микросервисов
```

**Почему это плохо**:
- Если один сервис скомпрометирован, все остальные тоже
- Невозможно отозвать доступ одному сервису

**Как правильно**:
- Unique credentials для каждого сервиса
- Service accounts с минимальными привилегиями

### 4. Отсутствие rate limiting

**Ошибка**:
```
API принимает неограниченное количество попыток логина
```

**Почему это плохо**:
- Brute-force атаки
- Credential stuffing
- DoS

**Как правильно**:
```
- Max 5 попыток логина в 5 минут
- Exponential backoff после неудачных попыток
- CAPTCHA после 3 неудачных попыток
```

### 5. Игнорирование certificate validation

**Ошибка**:
```python
requests.get('https://api.example.com', verify=False)
```

**Почему это плохо**:
- Уязвимо к MITM атакам
- Злоумышленник может перехватить трафик

**Как правильно**:
```python
requests.get('https://api.example.com', verify=True)
# Или указать конкретный CA bundle
requests.get('https://api.example.com', verify='/path/to/ca-bundle.crt')
```

### 6. Использование слабых криптографических алгоритмов

**Ошибка**:
```
MD5 для хеширования паролей
SHA1 для подписей
```

**Почему это плохо**:
- MD5 и SHA1 имеют collision attacks
- Быстро bruteforce-ятся

**Как правильно**:
```
Пароли: bcrypt, scrypt, Argon2
Подписи: RSA-2048+, ECDSA
Хеши: SHA-256, SHA-3
```

### 7. Отсутствие input validation

**Ошибка**:
```python
user_id = request.GET['id']
db.execute(f"DELETE FROM users WHERE id = {user_id}")
```

**Почему это плохо**:
- SQL injection
- Пользователь может отправить `1 OR 1=1`, удалить всех пользователей

**Как правильно**:
```python
user_id = int(request.GET['id'])  # Validate type
db.execute("DELETE FROM users WHERE id = ?", [user_id])  # Prepared statement
```

### 8. Логирование чувствительных данных

**Ошибка**:
```python
logger.info(f"User logged in: {username}, password: {password}")
```

**Почему это плохо**:
- Пароли попадают в логи
- Логи часто доступны широкому кругу людей

**Как правильно**:
```python
logger.info(f"User logged in: {username}")
# Никогда не логируй passwords, tokens, credit cards
```

### 9. Неправильная обработка JWT токенов

**Ошибка**:
```python
# Не проверяем подпись
payload = jwt.decode(token, options={"verify_signature": False})
```

**Почему это плохо**:
- Любой может создать поддельный токен
- Изменить payload (например, роль с `user` на `admin`)

**Как правильно**:
```python
payload = jwt.decode(token, secret_key, algorithms=['HS256'])
# Также проверяй exp, iss, aud
```

### 10. Забыли про session expiration

**Ошибка**:
```
JWT токен выдан на 10 лет
```

**Почему это плохо**:
- Если токен украден, он будет валиден 10 лет
- Невозможно отозвать (JWT stateless)

**Как правильно**:
```
Access token: 15 минут
Refresh token: 7 дней
Механизм ревокации для refresh tokens
```

---

## Заключение

Security & Trust Semantics — это не просто набор технологий, а **mindset** (образ мышления) при проектировании систем. Ключевые принципы:

### 🔑 Основные выводы

1. **Аутентификация и авторизация — это разное**
   - Authentication: "Кто ты?"
   - Authorization: "Что тебе разрешено?"
   - Оба необходимы, одно без другого бесполезно

2. **Доверие должно быть явным**
   - Определи trust boundaries
   - На границах — строгие проверки
   - Внутри границ — тоже проверки (defense in depth)

3. **Zero-Trust для критичных систем**
   - Never trust, always verify
   - Особенно актуально для облаков и микросервисов
   - Баланс между безопасностью и сложностью

4. **Byzantine tolerance только когда нужно**
   - Если все узлы доверенные — используй Crash Fault Tolerance (проще, быстрее)
   - BFT для multi-organization систем, blockchain, untrusted networks
   - Помни про `n ≥ 3f + 1`

5. **Безопасность — это процесс, не продукт**
   - Регулярные audits
   - Обновления и патчи
   - Обучение команды
   - Incident response plan

### 🎯 Что делать дальше

**Immediate actions** (можно сделать сегодня):
- Включи authentication на всех сервисах (Kafka, Redis, MongoDB, etc.)
- Убери default passwords и `trust` методы
- Включи TLS для данных в транзите
- Добавь rate limiting на auth endpoints

**Short-term** (в течение недели):
- Настрой RBAC/ACL с минимальными привилегиями
- Внедри secrets management (Vault, cloud secrets)
- Настрой audit logging
- Проведи security review текущих систем

**Long-term** (в течение месяцев):
- Внедри zero-trust architecture (service mesh)
- Автоматизируй security testing (SAST, DAST)
- Регулярные penetration tests
- Compliance сертификаты (если нужны)

### 📚 Дальнейшее изучение

- **OAuth 2.0 & OIDC**: стандарты для аутентификации и авторизации
- **SPIFFE/SPIRE**: универсальная идентичность для workloads
- **Open Policy Agent (OPA)**: policy engine для zero-trust
- **Consensus algorithms**: Raft, Paxos, PBFT для понимания fault tolerance
- **Cryptography**: основы для понимания mTLS, подписей, шифрования

Помни: **безопасность системы определяется её самым слабым звеном**. Даже если 99% системы защищены идеально, одна незащищённая точка может привести к полной компрометации. Будь vigilant (бдителен), проектируй с учётом угроз, и всегда предполагай, что атака возможна.

---

*Этот документ описывает фундаментальные концепции Security & Trust Semantics. Для глубокого понимания необходимо изучить специфику конкретных систем и протоколов, а также практиковаться в настройке безопасности реальных приложений.*
